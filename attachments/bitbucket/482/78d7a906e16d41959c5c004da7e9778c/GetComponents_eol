diff --git a/GetComponents b/GetComponents
index ac2f2cc..d058749 100755
--- a/GetComponents
+++ b/GetComponents
@@ -305,6 +305,9 @@ sub parse_list {
         my @lines = <$COMPONENT_LIST>;
         close($COMPONENT_LIST);
 
+        # convert CRNL and CR to newline (for lists generated by windows and macs)
+        map s/(\r\n|\r)/\n/gm, @lines;
+
         # handle includes
         my $i = -1;
         foreach my $line (@lines) {
@@ -349,9 +352,6 @@ sub parse_list {
     }
     my $orig_file = $file;
 
-    # convert CR to newline (for lists generated by windows)
-    $file =~ s/\r/\n/gm;
-
     # remove comments
     $file =~ s/^\s*#.*$//gm;
     $file =~ s/\n\n/\n/g;
