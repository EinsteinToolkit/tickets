Index: lib/sbin/CreateConfigurationBindings.pl
===================================================================
--- lib/sbin/CreateConfigurationBindings.pl	(revision 4829)
+++ lib/sbin/CreateConfigurationBindings.pl	(working copy)
@@ -244,13 +244,12 @@
   {
     if ($cfg->{"\U$thorn\E REQUIRES"} || $cfg->{"\U$thorn\E OPTIONAL"})
     {
-      $temp .= "#ifdef THORN_IS_$thorn\n";
       $temp .= "#include \"../Configuration/Thorns/cctki_$thorn.h\"\n";
-      $temp .= "#endif\n";
       $temp .= "\n";
     }
+    &WriteFile("../include/$thorn/cctk_Capabilities.h",\$temp);
   }
-  &WriteFile("../include/cctk_Capabilities.h",\$temp);
+  &WriteFile("../include/CactusBindings/cctk_Capabilities.h","#include \"../Configuration/Thorns/cctki_Cactus.h\"\n\n");
 }
 
 return 1;
Index: lib/sbin/CST
===================================================================
--- lib/sbin/CST	(revision 4829)
+++ lib/sbin/CST	(working copy)
@@ -142,7 +142,11 @@
 &CreateThornsHeaders(%source_thorns);
 &WriteFile("$bindings_dir/include/thornlist.h", \$thornsheader);
 &WriteFile("$bindings_dir/include/cctk_DefineThorn.h", \$definethornsheader);
-&WriteFile("$bindings_dir/include/definethisthorn.h", \$definethisthornheader);
+for my $header (@$definethisthornheader) {
+  my $thorn = $header->{thorn};
+  my $body = $header->{body};
+  &WriteFile("$bindings_dir/include/$thorn/definethisthorn.h", \$body);
+}
 
 # Create the header files used by the thorns
 &BuildHeaders($cctk_home,$bindings_dir,%interface_database);
@@ -484,25 +488,31 @@
 sub CreateThornsHeaders
 {
   my(%thorns) = @_;
-  my($header1,$header2,$header3,$thorn,$nthorns);
+  my($header1,$header2,@header3,$thorn,$nthorns);
 
   $header1 = "\/* List of compiled thorns in the code. *\/\n\n";
   $header1 .= "static char const *const thorn_name[] = {\n";
   $header2 = "\/* Defines for compiled thorns in the code. *\/\n\n";
-  $header3 = "\/* Defines for thorn this file is part of *\/\n\n";
 
+  my @thorns = keys %thorns;
+
   $nthorns = 0;
-  foreach $thorn (sort keys %thorns)
+  foreach $thorn (sort @thorns)
   {
     # Only place package_name/thorn_name in the file.
     $thorns{$thorn} =~ m:.*/(.*)/(.*):;
+    my ($arr,$th) = ($1,$2);
 
-    $header3 .= "#ifdef THORN_IS_$thorn\n" .
-                "#define CCTK_THORN $2\n" .
-                "#define CCTK_THORNSTRING \"$2\"\n" .
-                "#define CCTK_ARRANGEMENT $1\n" .
-                "#define CCTK_ARRANGEMENTSTRING \"$1\"\n" .
-                "#endif\n\n";
+    push @header3, {thorn => $thorn, body=>
+                "\/* Defines for thorn this file is part of *\/\n\n".
+                "#ifndef DEFINE_THIS_THORN_H\n".
+                "#define DEFINE_THIS_THORN_H 1\n".
+                "#define THORN_IS_$thorn 1\n".
+                "#define CCTK_THORN $th\n" .
+                "#define CCTK_THORNSTRING \"$th\"\n" .
+                "#define CCTK_ARRANGEMENT $arr\n" .
+                "#define CCTK_ARRANGEMENTSTRING \"$arr\"\n".
+                "#endif\n\n"};
 
     # Ignore the main sources for the other headers - they may confuse
     next if ($thorn eq 'Cactus');
@@ -515,8 +525,9 @@
 
   $header1 .= "\"\"};\n\n";
   $header1 .= "static int const nthorns = $nthorns;\n\n";
+  push @header3, {thorn=>"CactusBindings", body=>"#define THORN_IS_CactusBindings\n\n"};
 
-  return ($header1, $header2, $header3);
+  return ($header1, $header2, \@header3);
 }
 
 
Index: lib/sbin/CreateParameterBindings.pl
===================================================================
--- lib/sbin/CreateParameterBindings.pl	(revision 4829)
+++ lib/sbin/CreateParameterBindings.pl	(working copy)
@@ -154,13 +154,14 @@
   foreach $thorn (split(' ',$rhinterface_db->{'THORNS'}))
   {
     $dataout = &CreateFortranThornParameterBindings($thorn, $rhparameter_db, $rhinterface_db);
-    &WriteFile("include/${thorn}_FParameters.h",\$dataout);
+    mkdir("include/$thorn");
+    &WriteFile("include/${thorn}/FParameters.h",\$dataout);
 
     $implementation = $rhinterface_db->{"\U$thorn\E IMPLEMENTS"};
 
     @data = ();
     push(@data, '/*@@');
-    push(@data, "   \@header  ${thorn}_CParameters.h");
+    push(@data, "   \@header  ${thorn}/CParameters.h");
     push(@data, '   @author  Automatically generated by CreateParameterBindings.pl');
     push(@data, '   @desc');
     push(@data, "            Declares parameters of thorn $thorn");
@@ -172,6 +173,7 @@
     push(@data, "#ifndef _\U$thorn\E_PARAMETERS_H_");
     push(@data, "#define _\U$thorn\E_PARAMETERS_H_ 1");
     push(@data, '');
+    push(@data, '#include "CParameterStructNames.h"');
 
     if($header_files{'GLOBAL'})
     {
@@ -231,7 +233,7 @@
     push(@data, "\n");  # workaround for perl 5.004_04 to add a trailing newline
 
     $dataout = join ("\n", @data);
-    &WriteFile("include/${thorn}_CParameters.h",\$dataout);
+    &WriteFile("include/${thorn}/CParameters.h",\$dataout);
   }
 
 # Write this one to a temporary file and read it back in
@@ -253,48 +255,24 @@
 
   &WriteFile('include/CParameterStructNames.h',\$dataout);
 
-
-  @data = ();
-  push(@data, '#include "CParameterStructNames.h"');
-  push(@data, '');
   foreach $thorn (split(' ',$rhinterface_db->{'THORNS'}))
   {
-    push(@data, "#ifdef THORN\_IS\_$thorn");
-    push(@data, "#include \"${thorn}_CParameters.h\"");
-    push(@data, '#endif');
+    @data = ();
+    push(@data, '/* get the CCTK datatype definitions */');
+    push(@data, '#include "cctk_Types.h"');
     push(@data, '');
-  }
-  push(@data, "\n");  # workaround for perl 5.004_04 to add a trailing newline
-  $dataout = join ("\n", @data);
-  &WriteFile('include/CParameters.h',\$dataout);
-
-  @data = ();
-  foreach $thorn (split(' ',$rhinterface_db->{'THORNS'}))
-  {
-    push(@data, "#ifdef THORN\_IS\_$thorn");
-    push(@data, "#include \"${thorn}_FParameters.h\"");
+    push(@data, '#ifdef CCODE');
+    push(@data, "#include \"${thorn}/CParameters.h\"");
+    push(@data, '#elif FCODE');
+    push(@data, "#include \"${thorn}/FParameters.h\"");
     push(@data, '#endif');
-    push(@data, '');
+    push(@data, "\n");   # workaround for perl 5.004_04 to add a trailing newline
+
+    $dataout = join ("\n", @data);
+    &WriteFile("include/${thorn}/cctk_Parameters.h",\$dataout);
   }
-  push(@data, "\n");  # workaround for perl 5.004_04 to add a trailing newline
-  $dataout = join ("\n", @data);
-  &WriteFile('include/FParameters.h',\$dataout);
 
-  @data = ();
-  push(@data, '/* get the CCTK datatype definitions */');
-  push(@data, '#include "cctk_Types.h"');
-  push(@data, '');
-  push(@data, '#ifdef CCODE');
-  push(@data, '#include "CParameters.h"');
-  push(@data, '#elif FCODE');
-  push(@data, '#include "FParameters.h"');
-  push(@data, '#endif');
-  push(@data, "\n");   # workaround for perl 5.004_04 to add a trailing newline
 
-  $dataout = join ("\n", @data);
-  &WriteFile('include/cctk_Parameters.h',\$dataout);
-
-
   chdir $start_dir;
 }
 
Index: lib/sbin/CreateImplementationBindings.pl
===================================================================
--- lib/sbin/CreateImplementationBindings.pl	(revision 4829)
+++ lib/sbin/CreateImplementationBindings.pl	(working copy)
@@ -37,6 +37,21 @@
   }
 
   @thorns = sort split(' ', $rhinterface_db->{'THORNS'});
+  if(! -d 'include')
+  {
+    mkdir('include', 0755) || die 'Unable to create include directory';
+  }
+  if(! -d 'include/CactusBindings')
+  {
+    mkdir('include/CactusBindings', 0755) || die 'Unable to create include/CactusBindings directory';
+  }
+  my $thorn;
+  foreach $thorn (@thorns) {
+    if(! -d "include/$thorns") {
+      mkdir("include/$thorn",0755) || die "Unable to create include/$thorn directory";
+    }
+  }
+
   @data = map { "void CCTKi_BindingsThorn_$_(void);" } @thorns;
   push(@data, '');
 
Index: lib/sbin/GridFuncStuff.pl
===================================================================
--- lib/sbin/GridFuncStuff.pl	(revision 4829)
+++ lib/sbin/GridFuncStuff.pl	(working copy)
@@ -43,92 +43,660 @@
     WriteFile("include/$thorn\_Arguments.h",\$dataout);
   }
 
-  @data = ();
-  push(@data, '/*@@');
-  push(@data, '   @header  cctk_Arguments.h');
-  push(@data, '   @author  Automatically generated by GridFuncStuff.pl');
-  push(@data, '   @desc');
-  push(@data, '            Defines the CCTK_ARGUMENTS macro for all thorns');
-  push(@data, '   @enddesc');
-  push(@data, ' @@*/');
-  push(@data, '');
-  push(@data, '');
+  my @thorns = split(" ",$rhinterface_db->{"THORNS"});
+  foreach my $thorn (@thorns)
+  {
+    @data = ();
+    push(@data, '/*@@');
+    push(@data, '   @header  cctk_Arguments.h');
+    push(@data, '   @author  Automatically generated by GridFuncStuff.pl');
+    push(@data, '   @desc');
+    push(@data, '            Defines the CCTK_ARGUMENTS macro for all thorns');
+    push(@data, '   @enddesc');
+    push(@data, ' @@*/');
+    push(@data, '');
+    push(@data, '');
+    push(@data, "#include \"${thorn}_Arguments.h\"");
 
-  push(@data, '/* get the CCTK datatype definitions */');
-  push(@data, '#include "cctk_Types.h"');
-  push(@data, '');
-  push(@data, '#ifdef CCODE');
+    push(@data, '/* get the CCTK datatype definitions */');
+    push(@data, '#include "cctk_Types.h"');
+    push(@data, '');
+    push(@data, '#ifdef CCODE');
 
-  # Deprecated in 4.0 beta 17
-  push(@data, '/* Older configurations do not define CCTK_RESTRICT.');
-  push(@data, ' * Remove this section after 4.0 beta 16 has been released');
-  push(@data, ' */');
+    # Deprecated in 4.0 beta 17
+    push(@data, '/* Older configurations do not define CCTK_RESTRICT.');
+    push(@data, ' * Remove this section after 4.0 beta 16 has been released');
+    push(@data, ' */');
 
-  push(@data, '#ifndef CCTK_RESTRICT');
-  push(@data,   '#ifdef __cplusplus');
-  push(@data,     '#ifdef CCTK_CXX_RESTRICT');
-  push(@data,       '#define CCTK_RESTRICT CCTK_CXX_RESTRICT');
-  push(@data,     '#else');
-  push(@data,       '#define CCTK_RESTRICT');
-  push(@data,     '#endif');
-  push(@data,   '#else');
-  push(@data,     '#ifdef CCTK_C_RESTRICT');
-  push(@data,       '#define CCTK_RESTRICT CCTK_C_RESTRICT');
-  push(@data,     '#else');
-  push(@data,       '#define CCTK_RESTRICT');
-  push(@data,     '#endif');
-  push(@data,   '#endif');
-  push(@data, '#endif');
+    push(@data, '#ifndef CCTK_RESTRICT');
+    push(@data,   '#ifdef __cplusplus');
+    push(@data,     '#ifdef CCTK_CXX_RESTRICT');
+    push(@data,       '#define CCTK_RESTRICT CCTK_CXX_RESTRICT');
+    push(@data,     '#else');
+    push(@data,       '#define CCTK_RESTRICT');
+    push(@data,     '#endif');
+    push(@data,   '#else');
+    push(@data,     '#ifdef CCTK_C_RESTRICT');
+    push(@data,       '#define CCTK_RESTRICT CCTK_C_RESTRICT');
+    push(@data,     '#else');
+    push(@data,       '#define CCTK_RESTRICT');
+    push(@data,     '#endif');
+    push(@data,   '#endif');
+    push(@data, '#endif');
 
-  # End deprecated section
+    # End deprecated section
 
-  push(@data, '/* prototype for CCTKi_VarDataPtr() goes here');
-  push(@data, '   because we don\'t want to include another CCTK header file */');
-  push(@data, '#include "cGH.h"');
-  push(@data, '#ifdef __cplusplus');
-  push(@data, 'extern "C"');
-  push(@data, '#endif');
-  push(@data, 'void *CCTKi_VarDataPtr(const cGH *GH, int timelevel,');
-  push(@data, '                       const char *implementation, const char *varname);');
-  push(@data, '');
+    push(@data, '/* prototype for CCTKi_VarDataPtr() goes here');
+    push(@data, '   because we don\'t want to include another CCTK header file */');
+    push(@data, '#include "cGH.h"');
+    push(@data, '#ifdef __cplusplus');
+    push(@data, 'extern "C"');
+    push(@data, '#endif');
+    push(@data, 'void *CCTKi_VarDataPtr(const cGH *GH, int timelevel,');
+    push(@data, '                       const char *implementation, const char *varname);');
+    push(@data, '');
 
-  push(@data, '#define PASS_GROUPSIZE(group, dir)  CCTKGROUPNUM_##group >= 0 ? \\');
-  push(@data, '                                    CCTK_ArrayGroupSizeI(GH, dir, CCTKGROUPNUM_##group) : &_cctk_zero');
-  push(@data, '');
-  push(@data, '#define PASS_GROUPLEN(thorn, group) CCTKGROUPNUM_##group >= 0 ? \\');
-  push(@data, '                                    CCTKi_GroupLengthAsPointer(#thorn "::" #group) : &_cctk_zero');
-  push(@data, '');
-  push(@data, '#define PASS_REFERENCE(var, level)  CCTKARGNUM_##var >= 0 ? \\');
-  push(@data, '                                    GH->data[CCTKARGNUM_##var][level] : 0');
-  push(@data, '');
+    push(@data, '#define PASS_GROUPSIZE(group, dir)  CCTKGROUPNUM_##group >= 0 ? \\');
+    push(@data, '                                    CCTK_ArrayGroupSizeI(GH, dir, CCTKGROUPNUM_##group) : &_cctk_zero');
+    push(@data, '');
+    push(@data, '#define PASS_GROUPLEN(thorn, group) CCTKGROUPNUM_##group >= 0 ? \\');
+    push(@data, '                                    CCTKi_GroupLengthAsPointer(#thorn "::" #group) : &_cctk_zero');
+    push(@data, '');
+    push(@data, '#define PASS_REFERENCE(var, level)  CCTKARGNUM_##var >= 0 ? \\');
+    push(@data, '                                    GH->data[CCTKARGNUM_##var][level] : 0');
+    push(@data, '');
 
-  push(@data, '#define CCTK_ARGUMENTS CCTK_CARGUMENTS');
-  push(@data, '#define _CCTK_ARGUMENTS _CCTK_CARGUMENTS');
-  push(@data, '#define DECLARE_CCTK_ARGUMENTS DECLARE_CCTK_CARGUMENTS');
-  push(@data, '#endif');
-  push(@data, '');
-  push(@data, '#ifdef FCODE');
-  push(@data, '#define CCTK_ARGUMENTS CCTK_FARGUMENTS');
-  push(@data, '#define _CCTK_ARGUMENTS _CCTK_FARGUMENTS');
-  push(@data, '#define DECLARE_CCTK_ARGUMENTS DECLARE_CCTK_FARGUMENTS');
-  push(@data, '#endif');
+    push(@data, "#define CCTK_ARGUMENTS \U${thorn}_CARGUMENTS");
+    push(@data, '#define _CCTK_ARGUMENTS _CCTK_CARGUMENTS');
+    push(@data, "#define DECLARE_CCTK_ARGUMENTS DECLARE_\U${thorn}_CARGUMENTS");
+    push(@data, '#endif');
+    push(@data, '');
+    push(@data, '#ifdef FCODE');
+    push(@data, "#define CCTK_ARGUMENTS \U${thorn}_FARGUMENTS");
+    push(@data, '#define _CCTK_ARGUMENTS _CCTK_FARGUMENTS');
+    push(@data, "#define DECLARE_CCTK_ARGUMENTS DECLARE_\U${thorn}_FARGUMENTS");
+    push(@data, '#endif');
 
-  foreach my $thorn (split(" ",$rhinterface_db->{"THORNS"}))
-  {
     push(@data, '');
-    push(@data, "#ifdef THORN_IS_$thorn");
-    push(@data, "#include \"${thorn}_Arguments.h\"");
     push(@data, "#define CCTK_FARGUMENTS \U$thorn" . '_FARGUMENTS');
     push(@data, "#define DECLARE_CCTK_FARGUMENTS DECLARE_\U$thorn" . '_FARGUMENTS');
     push(@data, "#define CCTK_CARGUMENTS \U$thorn" . '_CARGUMENTS');
     push(@data, "#define DECLARE_CCTK_CARGUMENTS DECLARE_\U$thorn" . '_CARGUMENTS');
-    push(@data, '#endif');
+    push(@data, "\n");  # workaround for perl 5.004_04 to add a trailing newline
+
+    my $dataout = join ("\n", @data);
+    unless(-d "include/$thorn") {
+        mkdir("include/$thorn");
+    }
+    WriteFile("include/$thorn/cctk_Arguments.h",\$dataout);
+    if($thorn eq "Cactus") {
+      unless(-d "include/CactusBindings") {
+          mkdir("include/CactusBindings");
+      }
+      WriteFile("include/CactusBindings/cctk_Arguments.h",\$dataout);
+    }
+    @data = ();
+    # alternate
+    push @data, "#ifndef _CCTK_H_";
+    push @data, "#define _CCTK_H_ 1";
+    push @data, "#include \"${thorn}/definethisthorn.h\"";
+    push @data, "";
+    push @data, "/* Include prototypes for scheduled functions */";
+    push @data, "#include \"${thorn}/cctk_ScheduleFunctions.h\"";
+    push @data, "#include \"cctk_core.h\"";
+    push @data, "#endif";
+    push @data, "";
+    # CREATE CCTK_H
+    #push @data, "/*\@\@";
+    #push @data, "   \@header    cctk.h";
+    #push @data, "   \@date      Tue Jan 26 17:29:34 1999";
+    #push @data, "   \@author    Tom Goodale";
+    #push @data, "   \@desc";
+    #push @data, "              Main include file for the CCTK.";
+    #push @data, "              All thorns should include this.";
+    #push @data, "   \@enddesc";
+    #push @data, "   \@version   \$Header\$";
+    #push @data, " \@\@*/";
+    #push @data, "";
+    #push @data, "#ifndef _CCTK_H_";
+    #push @data, "#define _CCTK_H_ 1";
+    #push @data, "";
+    #push @data, "/* Grab the main configuration info. */";
+    #push @data, "#include \"cctk_Config.h\"";
+    #push @data, "";
+    #push @data, "/* Define which thorn the file is in */";
+    #push @data, "#include \"${thorn}/definethisthorn.h\"";
+    #push @data, "";
+    #push @data, "/* Include the constants */";
+    #push @data, "#include \"cctk_Constants.h\"";
+    #push @data, "";
+    #push @data, "/* Define some stuff */";
+    #push @data, "";
+    #push @data, "#ifdef FCODE";
+    #push @data, "";
+    #push @data, "#include \"cctk_Faces.h\"";
+    #push @data, "#include \"cctk_Interp.h\"";
+    #push @data, "#include \"cctk_Loop.h\"";
+    #push @data, "#include \"cctk_WarnLevel.h\"";
+    #push @data, "";
+    #push @data, "#define CCTK_PRINTSEPARATOR\\";
+    #push @data, "  print '(\"--------------------------------------------------------------------------------\")'";
+    #push @data, "";
+    #push @data, "#define _CCTK_FARGUMENTS \\";
+    #push @data, "cctk_dim,cctk_gsh,cctk_lsh,cctk_lbnd,cctk_ubnd,cctk_lssh,cctk_from,cctk_to,\\";
+    #push @data, "cctk_bbox,cctk_delta_time,cctk_time,cctk_delta_space,cctk_origin_space,\\";
+    #push @data, "cctk_levfac,cctk_levoff,cctk_levoffdenom,cctk_timefac,cctk_convlevel,\\";
+    #push @data, "cctk_convfac,cctk_nghostzones,cctk_iteration,cctkGH,\\";
+    #push @data, "cctk_lsh1,cctk_lsh2,cctk_lsh3";
+    #push @data, "";
+    #push @data, "#define _DECLARE_CCTK_ARGUMENTS _DECLARE_CCTK_FARGUMENTS";
+    #push @data, "#define _DECLARE_CCTK_FARGUMENTS &&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_dim,)&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_gsh,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_lsh,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_lbnd,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_ubnd,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_lssh,(CCTK_NSTAGGER*cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_from,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_to,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_bbox,(2*cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(CCTK_REAL,cctk_delta_time,)&&\\";
+    #push @data, "        CCTK_DECLARE(CCTK_REAL,cctk_time,)&&\\";
+    #push @data, "        CCTK_DECLARE(CCTK_REAL,cctk_delta_space,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(CCTK_REAL,cctk_origin_space,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_levfac,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_levoff,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_levoffdenom,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_timefac,)&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_convlevel,)&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_convfac,)&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_nghostzones,(cctk_dim))&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_iteration,)&&\\";
+    #push @data, "        CCTK_DECLARE(CCTK_POINTER,cctkGH,)&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_lsh1,)&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_lsh2,)&&\\";
+    #push @data, "        CCTK_DECLARE(INTEGER,cctk_lsh3,)&&";
+    #push @data, "";
+    #push @data, "#define CCTK_WARN(a,b) CCTK_Warn(a,__LINE__,__FORTRANFILE__,CCTK_THORNSTRING,b)";
+    #push @data, "";
+    #push @data, "#define CCTK_CoordRegisterSystem(a,b,c) CCTKi_CoordRegisterSystem(a,b,CCTK_THORNSTRING,c)";
+    #push @data, "";
+    #push @data, "/* John Shalf says that the operator .ne. needs to be enclosed by";
+    #push @data, "   spaces, because ANSI C preprocessors otherwise interpret the";
+    #push @data, "   character sequence \".0\" as preprocessor token */";
+    #push @data, "#define CCTK_EQUALS(a,b) (CCTK_Equals(a,b) .ne. 0)";
+    #push @data, "";
+    #push @data, "#define CCTK_PASS_FTOF CCTK_FARGUMENTS";
+    #push @data, "";
+    #push @data, "#define CCTK_ORIGIN_SPACE(x) (cctk_origin_space(x)+cctk_delta_space(x)/cctk_levfac(x)*cctk_levoff(x)/cctk_levoffdenom(x))";
+    #push @data, "#define CCTK_DELTA_SPACE(x) (cctk_delta_space(x)/cctk_levfac(x))";
+    #push @data, "#define CCTK_DELTA_TIME (cctk_delta_time/cctk_timefac)";
+    #push @data, "/* The \"stagger index\" stag is zero-based (0,1,...), the direction dim";
+    #push @data, "   is one-based in Fortran (1,2,3,...) */";
+    #push @data, "#define CCTK_LSSH(stag,dim) cctk_lssh(CCTK_LSSH_IDX(stag,dim))";
+    #push @data, "#define CCTK_LSSH_IDX(stag,dim) (1+(stag)+CCTK_NSTAGGER*((dim)-1))";
+    #push @data, "";
+    #push @data, "#ifdef F90CODE";
+    #push @data, "";
+    #push @data, "#define _DECLARE_CCTK_FUNCTIONS                   \\";
+    #push @data, "  external     CCTK_PointerTo                   &&\\";
+    #push @data, "  CCTK_POINTER CCTK_PointerTo                   &&\\";
+    #push @data, "  interface                                     &&\\";
+    #push @data, "     integer function CCTK_Equals (arg1, arg2)  &&\\";
+    #push @data, "       implicit none                            &&\\";
+    #push @data, "       CCTK_POINTER_TO_CONST arg1               &&\\";
+    #push @data, "       character(*) arg2                        &&\\";
+    #push @data, "     end function CCTK_Equals                   &&\\";
+    #push @data, "     integer function CCTK_MyProc (cctkGH)      &&\\";
+    #push @data, "       implicit none                            &&\\";
+    #push @data, "       CCTK_POINTER_TO_CONST cctkGH             &&\\";
+    #push @data, "     end function CCTK_MyProc                   &&\\";
+    #push @data, "     integer function CCTK_nProcs (cctkGH)      &&\\";
+    #push @data, "       implicit none                            &&\\";
+    #push @data, "       CCTK_POINTER_TO_CONST cctkGH             &&\\";
+    #push @data, "     end function CCTK_nProcs                   &&\\";
+    #push @data, "     integer function CCTK_IsThornActive (name) &&\\";
+    #push @data, "       implicit none                            &&\\";
+    #push @data, "       character(*) name                        &&\\";
+    #push @data, "     end function CCTK_IsThornActive            &&\\";
+    #push @data, "     CCTK_POINTER function CCTK_NullPointer ()  &&\\";
+    #push @data, "       implicit none                            &&\\";
+    #push @data, "     end function CCTK_NullPointer              &&\\";
+    #push @data, "  end interface                                 &&";
+    #push @data, "";
+    #push @data, "#else /* ! F90CODE */";
+    #push @data, "";
+    #push @data, "#define _DECLARE_CCTK_FUNCTIONS \\";
+    #push @data, "  integer      CCTK_Equals, CCTK_MyProc, CCTK_nProcs, CCTK_IsThornActive &&\\";
+    #push @data, "  external     CCTK_Equals, CCTK_MyProc, CCTK_nProcs, CCTK_IsThornActive &&\\";
+    #push @data, "  CCTK_POINTER CCTK_PointerTo, CCTK_NullPointer &&\\";
+    #push @data, "  external     CCTK_PointerTo, CCTK_NullPointer &&";
+    #push @data, "";
+    #push @data, "#endif /* ! F90CODE */";
+    #push @data, "";
+    #push @data, "#endif /*FCODE*/";
+    #push @data, "";
+    #push @data, "#ifdef CCODE";
+    #push @data, "";
+    #push @data, "/* get the definition of ptrdiff_t */";
+    #push @data, "#include <stddef.h>";
+    #push @data, "";
+    #push @data, "#include \"cGH.h\"";
+    #push @data, "";
+    #push @data, "#include \"cctk_ActiveThorns.h\"";
+    #push @data, "#include \"cctk_Banner.h\"";
+    #push @data, "#include \"cctk_Cache.h\"";
+    #push @data, "#include \"cctk_Coord.h\"";
+    #push @data, "#include \"cctk_Comm.h\"";
+    #push @data, "#include \"cctk_CommandLine.h\"";
+    #push @data, "#include \"cctk_Complex.h\"";
+    #push @data, "#include \"cctk_DebugDefines.h\"";
+    #push @data, "#include \"cctk_Faces.h\"";
+    #push @data, "#include \"cctk_File.h\"";
+    #push @data, "#include \"cctk_Flesh.h\"";
+    #push @data, "#include \"cctk_FortranString.h\"";
+    #push @data, "#include \"cctk_Functions.h\"";
+    #push @data, "#include \"cctk_GHExtensions.h\"";
+    #push @data, "#include \"cctk_Groups.h\"";
+    #push @data, "#include \"cctk_GroupsOnGH.h\"";
+    #push @data, "#include \"cctk_Interp.h\"";
+    #push @data, "#include \"cctk_IO.h\"";
+    #push @data, "#include \"cctk_IOMethods.h\"";
+    #push @data, "#include \"cctk_Loop.h\"";
+    #push @data, "#include \"cctk_Main.h\"";
+    #push @data, "#include \"cctk_Malloc.h\"";
+    #push @data, "#include \"cctk_Misc.h\"";
+    #push @data, "#include \"cctk_Parameter.h\"";
+    #push @data, "#include \"cctk_Reduction.h\"";
+    #push @data, "#include \"cctk_Stagger.h\"";
+    #push @data, "#include \"cctk_Sync.h\"";
+    #push @data, "#include \"cctk_Timers.h\"";
+    #push @data, "#include \"cctk_Termination.h\"";
+    #push @data, "#include \"cctk_WarnLevel.h\"";
+    #push @data, "";
+    #push @data, "/* Include prototypes for scheduled functions */";
+    #push @data, "#include \"${thorn}/cctk_ScheduleFunctions.h\"";
+    #push @data, "";
+    #push @data, "/* Include definitions provided by capabilities */";
+    #push @data, "#include \"cctk_Capabilities.h\"";
+    #push @data, "";
+    #push @data, "/*";
+    #push @data, " * routines/macros to compute the linear index";
+    #push @data, " * of a grid funtion element from its i/j/k dimensions";
+    #push @data, " *";
+    #push @data, " * These are defined as inline functions when the language supports this,";
+    #push @data, " * otherwise they are defined as macros.";
+    #push @data, " * For CCTK_DEBUG these are external C routines defined in DebugDefines.c.";
+    #push @data, " */";
+    #push @data, "";
+    #push @data, "#ifdef __cplusplus";
+    #push @data, "#  define HAVE_INLINE";
+    #push @data, "#else";
+    #push @data, "#  ifdef HAVE_CCTK_C_INLINE";
+    #push @data, "#    define HAVE_INLINE";
+    #push @data, "#  endif";
+    #push @data, "#endif";
+    #push @data, "";
+    #push @data, "#ifdef HAVE_INLINE";
+    #push @data, "";
+    #push @data, "/* The \"inline\" keyword is supported */";
+    #push @data, "";
+    #push @data, "static inline int CCTK_GFINDEX0D (const cGH *GH);";
+    #push @data, "static inline int CCTK_GFINDEX0D (const cGH *GH)";
+    #push @data, "{";
+    #push @data, "  return (0);";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_GFINDEX1D (const cGH *GH, int i);";
+    #push @data, "static inline int CCTK_GFINDEX1D (const cGH *GH, int i)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d cctk_lsh=[%d]\",";
+    #push @data, "                i, GH->cctk_lsh[0]);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i);";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_GFINDEX2D (const cGH *GH, int i, int j);";
+    #push @data, "static inline int CCTK_GFINDEX2D (const cGH *GH, int i, int j)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0] ||";
+    #push @data, "      j < 0 || j >= GH->cctk_lsh[1])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d j=%d cctk_lsh=[%d,%d]\",";
+    #push @data, "                i, j, GH->cctk_lsh[0], GH->cctk_lsh[1]);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i + GH->cctk_lsh[0]*j);";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_GFINDEX3D (const cGH *GH, int i, int j, int k);";
+    #push @data, "static inline int CCTK_GFINDEX3D (const cGH *GH, int i, int j, int k)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0] ||";
+    #push @data, "      j < 0 || j >= GH->cctk_lsh[1] ||";
+    #push @data, "      k < 0 || k >= GH->cctk_lsh[2])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d j=%d k=%d cctk_lsh=[%d,%d,%d]\",";
+    #push @data, "                i, j, k, GH->cctk_lsh[0], GH->cctk_lsh[1], GH->cctk_lsh[2]);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i + GH->cctk_lsh[0]*(j + GH->cctk_lsh[1]*k));";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_GFINDEX4D (const cGH *GH, int i, int j, int k, int l);";
+    #push @data, "static inline int CCTK_GFINDEX4D (const cGH *GH, int i, int j, int k, int l)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0] ||";
+    #push @data, "      j < 0 || j >= GH->cctk_lsh[1] ||";
+    #push @data, "      k < 0 || k >= GH->cctk_lsh[2] ||";
+    #push @data, "      l < 0 || l >= GH->cctk_lsh[3])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d j=%d k=%d l=%d cctk_lsh=[%d,%d,%d,%d]\",";
+    #push @data, "                i, j, k, l, GH->cctk_lsh[0], GH->cctk_lsh[1], GH->cctk_lsh[2], GH->cctk_lsh[3]);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i + GH->cctk_lsh[0]*(j + GH->cctk_lsh[1]*(k + GH->cctk_lsh[2]*l)));";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_VECTGFINDEX0D (const cGH *GH, int n);";
+    #push @data, "static inline int CCTK_VECTGFINDEX0D (const cGH *GH, int n)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (n < 0)";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Vector index out of bounds.  n=%d\",";
+    #push @data, "                n);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (n);";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_VECTGFINDEX1D (const cGH *GH, int i, int n);";
+    #push @data, "static inline int CCTK_VECTGFINDEX1D (const cGH *GH, int i, int n)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d cctk_lsh=[%d]\",";
+    #push @data, "                i, GH->cctk_lsh[0]);";
+    #push @data, "  }";
+    #push @data, "  if (n < 0)";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Vector index out of bounds.  n=%d\",";
+    #push @data, "                n);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i + GH->cctk_lsh[0]*n);";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_VECTGFINDEX2D (const cGH *GH, int i, int j, int n);";
+    #push @data, "static inline int CCTK_VECTGFINDEX2D (const cGH *GH, int i, int j, int n)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0] ||";
+    #push @data, "      j < 0 || j >= GH->cctk_lsh[1])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d j=%d cctk_lsh=[%d,%d]\",";
+    #push @data, "                i, j, GH->cctk_lsh[0], GH->cctk_lsh[1]);";
+    #push @data, "  }";
+    #push @data, "  if (n < 0)";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Vector index out of bounds.  n=%d\",";
+    #push @data, "                n);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i + GH->cctk_lsh[0]*(j + GH->cctk_lsh[1]*n));";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_VECTGFINDEX3D (const cGH *GH,";
+    #push @data, "                                      int i, int j, int k, int n);";
+    #push @data, "static inline int CCTK_VECTGFINDEX3D (const cGH *GH,";
+    #push @data, "                                      int i, int j, int k, int n)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0] ||";
+    #push @data, "      j < 0 || j >= GH->cctk_lsh[1] ||";
+    #push @data, "      k < 0 || k >= GH->cctk_lsh[2])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d j=%d k=%d cctk_lsh=[%d,%d,%d]\",";
+    #push @data, "                i, j, k, GH->cctk_lsh[0], GH->cctk_lsh[1], GH->cctk_lsh[2]);";
+    #push @data, "  }";
+    #push @data, "  if (n < 0)";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Vector index out of bounds.  n=%d\",";
+    #push @data, "                n);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i + GH->cctk_lsh[0]*(j + GH->cctk_lsh[1]*(k + GH->cctk_lsh[2]*n)));";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "static inline int CCTK_VECTGFINDEX4D (const cGH *GH,";
+    #push @data, "                                      int i, int j, int k, int l, int n);";
+    #push @data, "static inline int CCTK_VECTGFINDEX4D (const cGH *GH,";
+    #push @data, "                                      int i, int j, int k, int l, int n)";
+    #push @data, "{";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "  if (i < 0 || i >= GH->cctk_lsh[0] ||";
+    #push @data, "      j < 0 || j >= GH->cctk_lsh[1] ||";
+    #push @data, "      k < 0 || k >= GH->cctk_lsh[2] ||";
+    #push @data, "      l < 0 || l >= GH->cctk_lsh[3])";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Grid function index out of bounds.  i=%d j=%d k=%d l=%d cctk_lsh=[%d,%d,%d,%d]\",";
+    #push @data, "                i, j, k, l, GH->cctk_lsh[0], GH->cctk_lsh[1], GH->cctk_lsh[2], GH->cctk_lsh[3]);";
+    #push @data, "  }";
+    #push @data, "  if (n < 0)";
+    #push @data, "  {";
+    #push @data, "    CCTK_VWarn (CCTK_WARN_ABORT,";
+    #push @data, "                __LINE__, __FILE__, CCTK_THORNSTRING,";
+    #push @data, "                \"Vector index out of bounds.  n=%d\",";
+    #push @data, "                n);";
+    #push @data, "  }";
+    #push @data, "#endif";
+    #push @data, "  return (i + GH->cctk_lsh[0]*(j + GH->cctk_lsh[1]*(k + GH->cctk_lsh[2]*";
+    #push @data, "                                                    (l + GH->cctk_lsh[3]*n))));";
+    #push @data, "}";
+    #push @data, "";
+    #push @data, "#else /* ! defined(HAVE_INLINE) */";
+    #push @data, "";
+    #push @data, "#ifdef CCTK_DEBUG";
+    #push @data, "/* The \"inline\" keyword is not supported, and we want to debug */";
+    #push @data, "";
+    #push @data, "#define CCTK_GFINDEX0D CCTK_GFIndex0D";
+    #push @data, "#define CCTK_GFINDEX1D CCTK_GFIndex1D";
+    #push @data, "#define CCTK_GFINDEX2D CCTK_GFIndex2D";
+    #push @data, "#define CCTK_GFINDEX3D CCTK_GFIndex3D";
+    #push @data, "#define CCTK_GFINDEX4D CCTK_GFIndex4D";
+    #push @data, "#define CCTK_VECTGFINDEX0D CCTK_VectGFIndex0D";
+    #push @data, "#define CCTK_VECTGFINDEX1D CCTK_VectGFIndex1D";
+    #push @data, "#define CCTK_VECTGFINDEX2D CCTK_VectGFIndex2D";
+    #push @data, "#define CCTK_VECTGFINDEX3D CCTK_VectGFIndex3D";
+    #push @data, "#define CCTK_VECTGFINDEX4D CCTK_VectGFIndex4D";
+    #push @data, "";
+    #push @data, "#else /* ! defined(CCTK_DEBUG) */";
+    #push @data, "/* The \"inline\" keyword is not supported, and we want to optimise */";
+    #push @data, "";
+    #push @data, "#define CCTK_GFINDEX0D(GH)                      \\";
+    #push @data, "  (0)";
+    #push @data, "#define CCTK_GFINDEX1D(GH, i)                   \\";
+    #push @data, "  (i)";
+    #push @data, "#define CCTK_GFINDEX2D(GH, i, j)                \\";
+    #push @data, "  ((i) + (GH)->cctk_lsh[0] * (j))";
+    #push @data, "#define CCTK_GFINDEX3D(GH, i, j, k)             \\";
+    #push @data, "  ((i) + (GH)->cctk_lsh[0] *                    \\";
+    #push @data, "   ((j) + (GH)->cctk_lsh[1] * (k)))";
+    #push @data, "#define CCTK_GFINDEX4D(GH, i, j, k, l)          \\";
+    #push @data, "  ((i) + (GH)->cctk_lsh[0] *                    \\";
+    #push @data, "   ((j) + (GH)->cctk_lsh[1] *                   \\";
+    #push @data, "    ((k) + (GH)->cctk_lsh[2] * (l))))";
+    #push @data, "#define CCTK_VECTGFINDEX0D(GH, n)               \\";
+    #push @data, "  (n)";
+    #push @data, "#define CCTK_VECTGFINDEX1D(GH, i, n)            \\";
+    #push @data, "  ((i) + (GH)->cctk_lsh[0] * (n))";
+    #push @data, "#define CCTK_VECTGFINDEX2D(GH, i, j, n)         \\";
+    #push @data, "  ((i) + (GH)->cctk_lsh[0] *                    \\";
+    #push @data, "   ((j) + (GH)->cctk_lsh[1] * (n)))";
+    #push @data, "#define CCTK_VECTGFINDEX3D(GH, i, j, k, n)      \\";
+    #push @data, "  ((i) + (GH)->cctk_lsh[0] *                    \\";
+    #push @data, "   ((j) + (GH)->cctk_lsh[1] *                   \\";
+    #push @data, "    ((k) + (GH)->cctk_lsh[2] * (n))))";
+    #push @data, "#define CCTK_VECTGFINDEX4D(GH, i, j, k, l, n)   \\";
+    #push @data, "  ((i) + (GH)->cctk_lsh[0] *                    \\";
+    #push @data, "   ((j) + (GH)->cctk_lsh[1] *                   \\";
+    #push @data, "    ((k) + (GH)->cctk_lsh[2] *                  \\";
+    #push @data, "     ((l) + (GH)->cctk_lsh[3] * (n)))))";
+    #push @data, "";
+    #push @data, "#endif /* ! defined(CCTK_DEBUG) */";
+    #push @data, "";
+    #push @data, "#endif /* ! defined(HAVE_INLINE) */";
+    #push @data, "";
+    #push @data, "#undef HAVE_INLINE";
+    #push @data, "";
+    #push @data, "";
+    #push @data, "";
+    #push @data, "#define CCTK_PRINTSEPARATOR \\";
+    #push @data, "  'printf(\"--------------------------------------------------------------------------------\")'";
+    #push @data, "";
+    #push @data, "#define _DECLARE_CCTK_ARGUMENTS _DECLARE_CCTK_CARGUMENTS";
+    #push @data, "#define _DECLARE_CCTK_CARGUMENTS \\";
+    #push @data, "        CCTK_DECLARE_INIT(ptrdiff_t,cctki_dummy_int,0);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const,cctk_dim,cctkGH->cctk_dim);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_gsh,cctkGH->cctk_gsh);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_lsh,cctkGH->cctk_lsh);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_lbnd,cctkGH->cctk_lbnd);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_ubnd,cctkGH->cctk_ubnd);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_lssh,cctkGH->cctk_lssh);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_from,cctkGH->cctk_from);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_to,cctkGH->cctk_to);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_bbox,cctkGH->cctk_bbox);\\";
+    #push @data, "        CCTK_DECLARE_INIT(CCTK_REAL const,cctk_delta_time,cctkGH->cctk_delta_time);\\";
+    #push @data, "        CCTK_DECLARE_INIT(CCTK_REAL const,cctk_time,cctkGH->cctk_time);\\";
+    #push @data, "        CCTK_DECLARE_INIT(CCTK_REAL const *CCTK_RESTRICT const,cctk_delta_space,cctkGH->cctk_delta_space);\\";
+    #push @data, "        CCTK_DECLARE_INIT(CCTK_REAL const *CCTK_RESTRICT const,cctk_origin_space,cctkGH->cctk_origin_space);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_levfac,cctkGH->cctk_levfac);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_levoff,cctkGH->cctk_levoff);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_levoffdenom,cctkGH->cctk_levoffdenom);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const,cctk_timefac,cctkGH->cctk_timefac);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const,cctk_convlevel,cctkGH->cctk_convlevel);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const,cctk_convfac,cctkGH->cctk_convfac);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const *CCTK_RESTRICT const,cctk_nghostzones,cctkGH->cctk_nghostzones);\\";
+    #push @data, "        CCTK_DECLARE_INIT(int const,cctk_iteration,cctkGH->cctk_iteration);\\";
+    #push @data, "";
+    #push @data, "#define _INITIALISE_CCTK_C2F";
+    #push @data, "#define _DECLARE_CCTK_C2F";
+    #push @data, "#define _PASS_CCTK_C2F(xGH) &((xGH)->cctk_dim),\\";
+    #push @data, "                            (xGH)->cctk_gsh,(xGH)->cctk_lsh,\\";
+    #push @data, "                            (xGH)->cctk_lbnd,(xGH)->cctk_ubnd,\\";
+    #push @data, "                            (xGH)->cctk_lssh,\\";
+    #push @data, "                            (xGH)->cctk_from,(xGH)->cctk_to,\\";
+    #push @data, "                            (xGH)->cctk_bbox,\\";
+    #push @data, "                            &((xGH)->cctk_delta_time), &((xGH)->cctk_time),\\";
+    #push @data, "                            (xGH)->cctk_delta_space, (xGH)->cctk_origin_space,\\";
+    #push @data, "                            (xGH)->cctk_levfac,\\";
+    #push @data, "                            (xGH)->cctk_levoff,\\";
+    #push @data, "                            (xGH)->cctk_levoffdenom,\\";
+    #push @data, "                            &((xGH)->cctk_timefac),\\";
+    #push @data, "                            &((xGH)->cctk_convlevel),\\";
+    #push @data, "                            &((xGH)->cctk_convfac),\\";
+    #push @data, "                            (xGH)->cctk_nghostzones,\\";
+    #push @data, "                            &((xGH)->cctk_iteration),\\";
+    #push @data, "                            &(xGH),\\";
+    #push @data, "                            &(xGH)->cctk_lsh[0],\\";
+    #push @data, "                            &(xGH)->cctk_lsh[1],\\";
+    #push @data, "                            &(xGH)->cctk_lsh[2]";
+    #push @data, "#define _CCTK_C2F_PROTO     int const *,\\";
+    #push @data, "                            int const *, int const *,\\";
+    #push @data, "                            int const *, int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *, int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            CCTK_REAL const *, CCTK_REAL const *,\\";
+    #push @data, "                            CCTK_REAL const *, CCTK_REAL const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            cGH const *const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *,\\";
+    #push @data, "                            int const *";
+    #push @data, "";
+    #push @data, "#define CCTK_EQUALS(a,b) (CCTK_Equals((a),(b)))";
+    #push @data, "";
+    #push @data, "#define CCTK_PASS_CTOC cctkGH";
+    #push @data, "";
+    #push @data, "#define CCTK_ORIGIN_SPACE(x) (cctk_origin_space[x]+cctk_delta_space[x]/cctk_levfac[x]*cctk_levoff[x]/cctk_levoffdenom[x])";
+    #push @data, "#define CCTK_DELTA_SPACE(x) (cctk_delta_space[x]/cctk_levfac[x])";
+    #push @data, "#define CCTK_DELTA_TIME (cctk_delta_time/cctk_timefac)";
+    #push @data, "/* The \"stagger index\" stag is zero-based (0,1,...), the direction dim";
+    #push @data, "   is zero-based in C (0,1,2,...) */";
+    #push @data, "#define CCTK_LSSH(stag,dim) cctk_lssh[CCTK_LSSH_IDX(stag,dim)]";
+    #push @data, "#define CCTK_LSSH_IDX(stag,dim) ((stag)+CCTK_NSTAGGER*(dim))";
+    #push @data, "";
+    #push @data, "#define CCTK_WARN(a,b) CCTK_Warn(a,__LINE__,__FILE__,CCTK_THORNSTRING,b)";
+    #push @data, "";
+    #push @data, "#define CCTK_MALLOC(s) CCTKi_Malloc(s,__LINE__,__FILE__)";
+    #push @data, "#define CCTK_FREE(p) CCTKi_Free(p)";
+    #push @data, "";
+    #push @data, "#endif /*CCODE*/";
+    #push @data, "";
+    #push @data, "#define CCTK_INFO(a) CCTK_Info(CCTK_THORNSTRING,(a))";
+    #push @data, "#define CCTK_PARAMWARN(a) CCTK_ParamWarn(CCTK_THORNSTRING,(a))";
+    #push @data, "";
+    #push @data, "/*";
+    #push @data, "#define CCTK_MAKESTRING(x) CCTK_REALSTRING(x)";
+    #push @data, "#define CCTK_REALSTRING(x) #x";
+    #push @data, "#define CCTK_WARN(a,b) CCTK_Warn(a,CCTK_MAKESTRING(CCTK_THORN),b,__LINE__,__FILE__)";
+    #push @data, "*/";
+    #push @data, "";
+    #push @data, "";
+    #push @data, "#endif";
+    #push @data, "";
+    # END CREATE CCTK_H
+    $dataout = join ("\n", @data);
+    WriteFile("include/$thorn/cctk.h",\$dataout);
+    if($thorn eq "Cactus") {
+      unless(-d "include/CactusBindings") {
+          mkdir("include/CactusBindings");
+      }
+      WriteFile("include/CactusBindings/cctk.h",\$dataout);
+    }
   }
-  push(@data, "\n");  # workaround for perl 5.004_04 to add a trailing newline
 
-  my $dataout = join ("\n", @data);
-  WriteFile("include/cctk_Arguments.h",\$dataout);
-
   if(! -d "Variables")
   {
     mkdir("Variables", 0755) || die "Unable to create Variables directory";
@@ -189,16 +757,17 @@
     push(@data, '');
     push(@data, '');
 
-    push(@data, "#define THORN_IS_$thorn 1");
+    #push(@data, "#define THORN_IS_$thorn 1");
     push(@data, '');
     push(@data, '#include <stddef.h>');
     push(@data, '');
-    push(@data, '#include "cctk.h"');
-    push(@data, '#include "cctk_Arguments.h"');
-    push(@data, '#include "cctk_Parameter.h"');
+    push(@data, "#include \"$thorn/cctk.h\"");
+    push(@data, "#include \"$thorn/cctk_Arguments.h\"");
+    push(@data, "#include \"cctk_Parameter.h\"");
     push(@data, '#include "cctk_WarnLevel.h"');
     push(@data, '#include "cctki_Groups.h"');
     push(@data, '#include "cctki_FortranWrappers.h"');
+    push(@data, "#include \"$thorn/cctk_ScheduleFunctions.h\"");
     push(@data, '');
 
     push(@data, "int CactusBindingsVariables_${thorn}_Initialise(void);");
@@ -377,20 +946,20 @@
           $sep = ',';
           if ($gtype ne 'GF')
           {
-            if($block eq 'PRIVATE')
-            {
+          if($block eq 'PRIVATE')
+          {
 # FIXME: quick hack to shorten argument names
 #            $arguments{"cctkv$dim$group"} = "(STORAGESIZE($thorn\::$group, $dim))";
-              $arguments{"X$dim$group"} = "(STORAGESIZE($thorn\::$group, $dim))";
-            }
-            else
-            {
+            $arguments{"X$dim$group"} = "(STORAGESIZE($thorn\::$group, $dim))";
+          }
+          else
+          {
 # FIXME: quick hack to shorten argument names
 #            $arguments{"cctkv$dim$group"} = "(STORAGESIZE($imp\::$group, $dim))";
-              $arguments{"X$dim$group"} = "(STORAGESIZE($imp\::$group, $dim))";
-            }
+            $arguments{"X$dim$group"} = "(STORAGESIZE($imp\::$group, $dim))";
           }
         }
+        }
         if(defined($vararraysize) && $compactgroup == 0)
         {
           $type .= "$sep${group}_length";
Index: lib/sbin/CreateFunctionBindings.pl
===================================================================
--- lib/sbin/CreateFunctionBindings.pl	(revision 4829)
+++ lib/sbin/CreateFunctionBindings.pl	(working copy)
@@ -282,7 +282,6 @@
 ###
 
   $dataout = &ThornMasterIncludes($function_db);
-  &WriteFile('include/cctk_Functions.h',\$dataout);
 
 ###
 # Create the prototype header file for all thorns that USE a
@@ -1757,50 +1756,52 @@
   use strict;
 
   my %function_db = %{$_[0]};
+  my @thorns = keys %function_db;
 
-  my(@data) = ();
+  my $thorn;
+  foreach $thorn (sort @thorns)
+  {
+    my(@data) = ();
 
-  # Header Data
-  push(@data, '/*@@');
-  push(@data, '   @header  cctk_Functions.h');
-  push(@data, '   @author  Automatically generated by CreateFunctionBindings.pl');
-  push(@data, '   @desc');
-  push(@data, '            Prototypes for overloaded functions used by all thorns');
-  push(@data, '   @enddesc');
-  push(@data, '  @@*/');
-  push(@data, '');
-  push(@data, '');
+    # Header Data
+    push(@data, '/*@@');
+    push(@data, '   @header  $thorn/cctk_Functions.h');
+    push(@data, '   @author  Automatically generated by CreateFunctionBindings.pl');
+    push(@data, '   @desc');
+    push(@data, '            Prototypes for overloaded functions used by all thorns');
+    push(@data, '   @enddesc');
+    push(@data, '  @@*/');
+    push(@data, '');
+    push(@data, '');
 
-  push(@data, '#ifndef _CCTK_FUNCTIONALIASES_H_');
-  push(@data, '#define _CCTK_FUNCTIONALIASES_H_ 1');
-  push(@data, '');
+    push(@data, '#ifndef _CCTK_FUNCTIONALIASES_H_');
+    push(@data, '#define _CCTK_FUNCTIONALIASES_H_ 1');
+    push(@data, '');
 
-  push(@data, '#ifdef CCODE');
-  push(@data, '#ifdef __cplusplus');
-  push(@data, 'extern "C" {');
-  push(@data, '#endif');
-  push(@data, '  CCTK_INT CCTK_IsFunctionAliased(const char *function);');
-  push(@data, '#ifdef __cplusplus');
-  push(@data, '}');
-  push(@data, '#endif');
-  push(@data, '#endif');
-  push(@data, '');
+    push(@data, '#ifdef CCODE');
+    push(@data, '#ifdef __cplusplus');
+    push(@data, 'extern "C" {');
+    push(@data, '#endif');
+    push(@data, '  CCTK_INT CCTK_IsFunctionAliased(const char *function);');
+    push(@data, '#ifdef __cplusplus');
+    push(@data, '}');
+    push(@data, '#endif');
+    push(@data, '#endif');
+    push(@data, '');
 
-  my $thorn;
-
-  foreach $thorn (sort keys %function_db)
-  {
-    push(@data, "#ifdef THORN_IS_$thorn");
     push(@data, "#include \"${thorn}_Prototypes.h\"");
     push(@data, "#define DECLARE_CCTK_FUNCTIONS DECLARE_\U$thorn\E_FUNCTIONS");
-    push(@data, '#endif');
-    push(@data, '');
-  }
 
-  push(@data, '#endif  /* _CCTK_FUNCTIONALIASES_H_ */');
-  push(@data, "\n");   # workaround for perl 5.004_04 to add a trailing newline
+    push(@data, '#endif  /* _CCTK_FUNCTIONALIASES_H_ */');
+    push(@data, "\n");   # workaround for perl 5.004_04 to add a trailing newline
 
-  return join ("\n",@data);
+    my $dataout = join ("\n",@data);
+    mkdir("include/${thorn}");
+    &WriteFile("include/${thorn}/cctk_Functions.h",\$dataout);
+    if($thorn eq "Cactus") {
+      &WriteFile("include/CactusBindings/cctk_Functions.h",\$dataout);
+    }
+  }
 }
 
 #/*@@
Index: lib/sbin/CreateScheduleBindings.pl
===================================================================
--- lib/sbin/CreateScheduleBindings.pl	(revision 4829)
+++ lib/sbin/CreateScheduleBindings.pl	(working copy)
@@ -53,21 +53,12 @@
     $file_list .= " Schedule$thorn.c";
 
     $rsbuffer = &ScheduleCreateInterfaceFile($thorn, $rhinterface_db, $rhschedule_db);
-    &WriteFile("../include/${thorn}_Schedule.h",\$rsbuffer);
+    &WriteFile("../include/${thorn}/cctk_ScheduleFunctions.h",\$rsbuffer);
+    if($thorn eq "Cactus") {
+        &WriteFile("../include/CactusBindings/cctk_ScheduleFunctions.h",\$rsbuffer);
+    }
   }
 
-  @data = ();
-  foreach $thorn (split(' ',$rhinterface_db->{'THORNS'}))
-  {
-    push(@data, "#ifdef THORN\_IS\_$thorn");
-    push(@data, "#include \"${thorn}_Schedule.h\"");
-    push(@data, '#endif');
-    push(@data, '');
-  }
-  push(@data, "\n");  # workaround for perl 5.004_04 to add a trailing newline
-  $dataout = join ("\n", @data);
-  &WriteFile('../include/cctk_ScheduleFunctions.h',\$dataout);
-
   $rsbuffer = &ScheduleCreateBindings($rhinterface_db, $rhschedule_db);
   &WriteFile("BindingsSchedule.c", \$rsbuffer);
   $file_list .= " BindingsSchedule.c";
@@ -115,7 +106,7 @@
   # Process each schedule block
   
   my @data = ();
-  push (@data, '#include "cctk_Arguments.h"');
+  push (@data, "#include \"$thorn/cctk_Arguments.h\"");
   
   for (my $block = 0;
        $block < $rhschedule_db->{"\U$thorn\E N_BLOCKS"};
@@ -292,11 +283,13 @@
   push(@data, '   @enddesc');
   push(@data, '@@*/');
   push(@data, '');
-  push(@data, "#define THORN_IS_$thorn");
+  #push(@data, "#define THORN_IS_$thorn");
+  #push(@data, "#define CCTK_THORNSTRING \"$thorn\"");
   push(@data, '');
-  push(@data, '#include "cctk.h"');
-  push(@data, '#include "cctk_Parameters.h"');
+  push(@data, "#include \"$thorn/cctk.h\"");
+  push(@data, "#include \"$thorn/CParameters.h\"");
   push(@data, '#include "cctki_ScheduleBindings.h"');
+  push(@data, "#include \"$thorn/cctk_ScheduleFunctions.h\"");
   push(@data, '');
   #push(@data, '/* prototypes for schedule bindings functions to be registered */');
   #push(@data, '/* Note that this is a cheat, we just need a function pointer. */');
Index: lib/make/make.config.defn.in
===================================================================
--- lib/make/make.config.defn.in	(revision 4829)
+++ lib/make/make.config.defn.in	(working copy)
@@ -179,7 +179,8 @@
 export USE_RANLIB = @USE_RANLIB@
 
 # Exclude some files from the dependency lists
-export DEP_EXCLUDE_FILES = cctk_Functions\.h|CParameterStructNames\.h|cctk_Arguments\.h|cctk_ScheduleFunctions\.h|definethisthorn\.h|FParameters.h|CParameters\.h|cctk_Capabilities\.h
+#export DEP_EXCLUDE_FILES = cctk_Functions\.h|CParameterStructNames\.h|cctk_Arguments\.h|cctk_ScheduleFunctions\.h|definethisthorn\.h|FParameters.h|CParameters\.h|cctk_Capabilities\.h
+#export DEP_EXCLUDE_FILES =
 
 # Command used to get the working directory
 export GET_WD = @GET_WD@
Index: lib/make/make.subdir
===================================================================
--- lib/make/make.subdir	(revision 4829)
+++ lib/make/make.subdir	(working copy)
@@ -25,10 +25,8 @@
 endif
 
 # Add appropriate include lines
-INC_DIRS += $(SRCDIR) $(SRCDIR)/include 
+INC_DIRS += $(SRCDIR) $(SRCDIR)/include  $(BINDINGS_DIR)/include/$(THORN)
 
-EXTRA_DEFINES += THORN_IS_$(THORN) 
-
 # Include the subdirectory's local include data
 -include $(SRCDIR)/make.code.defn
 
Index: lib/make/make.config.rules.in
===================================================================
--- lib/make/make.config.rules.in	(revision 4829)
+++ lib/make/make.config.rules.in	(working copy)
@@ -62,12 +62,12 @@
 # Define how to do dependencies
 ifeq ($(strip $(PERL_BACKUP_NECESSARY)),)
 define DEPENDENCY_FIXER
-	$(PERL) -pi -e "s,([^:]*),$(basename $@).o $(basename $@).d, if(m/: /);\
+	#$(PERL) -pi -e "s,([^:]*),$(basename $@).o $(basename $@).d, if(m/: /);\
 	 s:\s+\S*[/\\\]($(DEP_EXCLUDE_FILES))\b::g" $@
 endef
 else
 define DEPENDENCY_FIXER
-	$(PERL) -pi.bak -e "s,([^:]*),$(basename $@).o $(basename $@).d, if(m/: /);\
+	#$(PERL) -pi.bak -e "s,([^:]*),$(basename $@).o $(basename $@).d, if(m/: /);\
 	 s:\s+\S*[/\\\]($(DEP_EXCLUDE_FILES))\b::g" $@
 	rm $@.bak
 endef
Index: lib/make/make.thornlib
===================================================================
--- lib/make/make.thornlib	(revision 4829)
+++ lib/make/make.thornlib	(working copy)
@@ -35,13 +35,11 @@
 endif
 
 # Add appropriate include lines
-INC_DIRS += $(SYS_INC_DIRS) $(SRCDIR) $(SRCDIR)/include $(CONFIG) $(BINDINGS_DIR)/include $(CCTK_HOME)/src/include $(CCTK_HOME)/arrangements $(BINDINGS_DIR)/Configuration/Thorns
+INC_DIRS += $(SYS_INC_DIRS) $(SRCDIR) $(SRCDIR)/include $(CONFIG) $(BINDINGS_DIR)/include $(CCTK_HOME)/src/include $(CCTK_HOME)/arrangements $(BINDINGS_DIR)/Configuration/Thorns $(BINDINGS_DIR)/include/$(THORN)
 
 # Allow subdirectories to inherit these include directories
 export INC_DIRS
 
-EXTRA_DEFINES += THORN_IS_$(THORN) 
-
 # Define some make variables
 PRE_WRAPPER = make.pre
 POST_WRAPPER = make.post
Index: src/main/SetupCache.c
===================================================================
--- src/main/SetupCache.c	(revision 4829)
+++ src/main/SetupCache.c	(working copy)
@@ -8,6 +8,8 @@
    @version   $Id$
  @@*/
 
+#include "definethisthorn.h"
+
 #include "cctk_Config.h"
 #include "cctk_Flesh.h"
 #include "cctk_Parameters.h"
Index: src/main/CactusDefaultEvolve.c
===================================================================
--- src/main/CactusDefaultEvolve.c	(revision 4829)
+++ src/main/CactusDefaultEvolve.c	(working copy)
@@ -13,6 +13,8 @@
 #include <stdio.h>
 #include <stdlib.h>
 
+#include "definethisthorn.h"
+
 #include "cctk_Flesh.h"
 #include "cctk_Parameters.h"
 #include "cctk_Groups.h"
Index: src/main/ShutdownCactus.c
===================================================================
--- src/main/ShutdownCactus.c	(revision 4829)
+++ src/main/ShutdownCactus.c	(working copy)
@@ -8,6 +8,8 @@
    @version   $Id$
  @@*/
 
+#include "definethisthorn.h"
+
 #include <stdio.h>
 #include <stdlib.h>
 
