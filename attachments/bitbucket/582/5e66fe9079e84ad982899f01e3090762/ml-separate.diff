diff --git a/m/McLachlan_BSSN.m b/m/McLachlan_BSSN.m
index 927b894..1e0e74b 100644
--- a/m/McLachlan_BSSN.m
+++ b/m/McLachlan_BSSN.m
@@ -1,6 +1,8 @@
 $Path = Join[$Path, {"../../../repos/Kranc/Tools/CodeGen",
                      "../../../repos/Kranc/Tools/MathematicaMisc"}];
 
+$RecursionLimit = 1000;
+
 Get["KrancThorn`"];
 
 SetEnhancedTimes[False];
@@ -40,7 +42,7 @@ derivatives =
   PDdissipationNth[i_] ->
     spacing[i]^(derivOrder+1) / 2^(derivOrder+2) *
     StandardCenteredDifferenceOperator[derivOrder+2,derivOrder/2+1,i],
-  
+
 (* PD: These come from my mathematica notebook
    "Upwind-Kranc-Convert.nb" that converts upwinding finite
    differencing operators generated by
@@ -192,8 +194,13 @@ PDus   = PDupwindNthSymm;
 PDdiss = PDdissipationNth;
 
 If [splitUpwindDerivs,
-    Upwind[dir_, var_, idx_] := dir PDua[var,idx] + Abs[dir] PDus[var,idx],
-    Upwind[dir_, var_, idx_] := dir PDu[var,idx]];
+    Upwind[dir_, var_, idx_] :=
+       Parenthesis[dir PDua[var,idx] + Abs[dir] PDus[var,idx]],
+    Upwind[dir_, var_, idx_] :=
+       Parenthesis[dir PDu[var,idx]]];
+
+Dissipation[var_] :=
+   Parenthesis[EpsDiss (PDdiss[var,1] + PDdiss[var,2] + PDdiss[var,3])];
 
 
 
@@ -214,8 +221,7 @@ Map [DefineTensor,
       e4phi, em4phi, ddetg, detgt, gtu, ddetgt, dgtu, ddgtu, Gtl, Gtlu, Gt,
       Rt, Rphi, gK,
       T00, T0, T, rho, S,
-      x, y, z, r,
-      epsdiss}];
+      x, y, z, r}];
 
 (* NOTE: It seems as if Lie[.,.] did not take these tensor weights
    into account.  Presumably, CD[.,.] and CDt[.,.] don't do this either.  *)
@@ -353,25 +359,44 @@ initialCalc =
 
 (******************************************************************************)
 (* Split a calculation *)
+(* Return a calculation that evaluates only those variables listed in LHSVars,
+   and contains only those RHS terms matching RHSpattern. If addLHS is set,
+   add the LHS to the RHS. *)
 (******************************************************************************)
 
-PartialCalculation[calc_, suffix_, updates_, evolVars_] :=
+(* TODO: simplify listing all variables *)
+(* TODO: simplify negating a pattern *)
+
+PartialCalculation[calc1_, suffix_, updates_, LHSVars_, RHSpattern_, addLHS_] :=
 Module[
-  {name, calc1, replaces, calc2, vars, patterns, eqs, calc3},
+  {calc, name, replaces, vars, patterns, eqs, select},
+  calc     = calc1;
   (* Add suffix to name *)
   name     = lookup[calc, Name] <> suffix;
-  calc1    = mapReplace[calc, Name, name];
-  (* Replace some entries in the calculation *)
-  (* replaces = Map[Function[rule, mapReplace[#, rule[[1]], rule[[2]]]&], updates]; *)
-  replaces = updates //. (lhs_ -> rhs_) -> (mapReplace[#, lhs, rhs]&);
-  calc2 = Apply[Composition, replaces][calc1];
+  calc     = mapReplace[calc, Name, name];
+  (* Replace or add some entries in the calculation *)
+  replaces = updates //. (lhs_ -> rhs_) -> (mapReplaceAdd[#, lhs, rhs]&);
+  calc     = Apply[Composition, replaces][calc];
   (* Remove unnecessary equations *)
-  vars     = Join[evolVars, lookup[calc2, Shorthands]];
+  vars     = Join[LHSVars, lookup[calc, Shorthands]];
   patterns = Replace[vars, {    Tensor[n_,__]  ->     Tensor[n,__] ,
                             dot[Tensor[n_,__]] -> dot[Tensor[n,__]]}, 1];
   eqs      = FilterRules[lookup[calc, Equations], patterns];
-  calc3    = mapReplace[calc2, Equations, eqs];
-  calc3
+  calc     = mapReplace[calc, Equations, eqs];
+  (* Remove unnecessary RHS terms *)
+  select[p_, e_] := 
+     Switch[e,
+            IfThen[__], MapAt[select[p, #] &, e, {{2}, {3}}],
+            _Plus, Map[select[p, #] &, e],
+            p, e,
+            _, 0];
+  eqs      = Map[Replace[#,
+                         (lhs_ -> rhs_) :> (lhs -> If[addLHS, lhs, 0] +
+                                                   select[RHSpattern, rhs])] &,
+                 lookup[calc, Equations]];
+  calc     = mapReplace[calc, Equations, eqs];
+  (* Done *)
+  calc
 ];
 
 
@@ -432,15 +457,14 @@ convertFromADMBaseGammaCalc =
                     (PD[gt[lb,ld],lc] + PD[gt[lc,ld],lb] - PD[gt[lb,lc],ld]),
     Xt[ua] -> gtu[ub,uc] Gt[ua,lb,lc],
     
-(*
-    A -> - admdtalpha / (harmonicF alpha^harmonicN) (LapseAdvectionCoeff - 1),
-*)
     (* If LapseACoeff=0, then A is not evolved, in the sense that it
        does not influence the time evolution of other variables.  *)
-    A -> IfThen [LapseACoeff != 0,
+    A -> IfThen [LapseACoeff,
                  1 / (- harmonicF alpha^harmonicN)
                  (+ admdtalpha
-                  - LapseAdvectionCoeff Upwind[beta[ua], alpha, la]),
+                  - IfThen[LapseAdvectionCoeff,
+                           Upwind[beta[ua], alpha, la],
+                           0]),
                  0],
     
     theta -> thetaExpr,
@@ -451,7 +475,9 @@ convertFromADMBaseGammaCalc =
     B[ua] -> IfThen [ShiftGammaCoeff ShiftBCoeff != 0,
                      1 / (theta ShiftGammaCoeff)
                      (+ admdtbeta[ua]
-                      - ShiftAdvectionCoeff Upwind[beta[ub], beta[ua], lb]),
+                      - IfThen[ShiftAdvectionCoeff,
+                               Upwind[beta[ub], beta[ua], lb],
+                               0]),
                      0]
   }
 };
@@ -510,19 +536,15 @@ convertToADMBaseDtLapseShiftCalc =
     theta -> thetaExpr,
     
     (* see RHS *)
-(*
-    admdtalpha -> - harmonicF alpha^harmonicN
-                    ((1 - LapseAdvectionCoeff) A + LapseAdvectionCoeff trK)
-                  + LapseAdvectionCoeff beta[ua] PDu[alpha,la],
-*)
-    admdtalpha -> - harmonicF alpha^harmonicN
-                    (+ LapseACoeff       A
-                     + (1 - LapseACoeff) trK)
-                  + LapseAdvectionCoeff Upwind[beta[ua], alpha, la],
+    admdtalpha -> - harmonicF alpha^harmonicN IfThen[LapseACoeff, A, trK]
+                  + IfThen[LapseAdvectionCoeff, Upwind[beta[ua], alpha, la], 0],
     admdtbeta[ua] -> + theta ShiftGammaCoeff
-                       (+ ShiftBCoeff        B[ua]
-                        + (1 - ShiftBCoeff) (Xt[ua] - eta BetaDriver beta[ua]))
-                     + ShiftAdvectionCoeff Upwind[beta[ub], beta[ua], lb]
+                       IfThen[ShiftBCoeff,
+                              B[ua],
+                              Xt[ua] - eta BetaDriver beta[ua]]
+                     + IfThen[ShiftAdvectionCoeff,
+                              Upwind[beta[ub], beta[ua], lb],
+                              0]
   }
 };
 
@@ -539,16 +561,11 @@ convertToADMBaseDtLapseShiftBoundaryCalc =
     theta -> thetaExpr,
     
     (* see RHS, but omit derivatives near the boundary *)
-(*
-    admdtalpha -> - harmonicF alpha^harmonicN
-                    ((1 - LapseAdvectionCoeff) A + LapseAdvectionCoeff trK),
-*)
-    admdtalpha -> - harmonicF alpha^harmonicN
-                    (+ LapseACoeff       A
-                     + (1 - LapseACoeff) trK),
+    admdtalpha -> - harmonicF alpha^harmonicN IfThen[LapseACoeff, A, trK],
     admdtbeta[ua] -> + theta ShiftGammaCoeff
-                       (+ ShiftBCoeff        B[ua]
-                        + (1 - ShiftBCoeff) (Xt[ua] - eta BetaDriver beta[ua]))
+                       IfThen[ShiftBCoeff,
+                              B[ua],
+                              Xt[ua] - eta BetaDriver beta[ua]]
   }
 };
 
@@ -567,16 +584,11 @@ convertToADMBaseFakeDtLapseShiftCalc =
     (* see RHS, but omit derivatives everywhere (which is wrong, but
        faster, since it does not require synchronisation or boundary
        conditions) *)
-(*
-    admdtalpha -> - harmonicF alpha^harmonicN
-                    ((1 - LapseAdvectionCoeff) A + LapseAdvectionCoeff trK),
-*)
-    admdtalpha -> - harmonicF alpha^harmonicN
-                    (+ LapseACoeff       A
-                     + (1 - LapseACoeff) trK),
+    admdtalpha -> - harmonicF alpha^harmonicN IfThen[LapseACoeff, A, trK],
     admdtbeta[ua] -> + theta ShiftGammaCoeff
-                       (+ ShiftBCoeff        B[ua]
-                        + (1 - ShiftBCoeff) (Xt[ua] - eta BetaDriver beta[ua]))
+                       IfThen[ShiftBCoeff,
+                              B[ua],
+                              Xt[ua] - eta BetaDriver beta[ua]]
   }
 };
 
@@ -588,6 +600,8 @@ evolCalc =
 {
   Name -> BSSN <> "_RHS",
   Schedule -> {"IN " <> BSSN <> "_evolCalcGroup"},
+  ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "never"},
+                            {"split_rhs_terms", "never"}},
   (*
   Where -> Interior,
   *)
@@ -602,7 +616,7 @@ evolCalc =
                  Atm[ua,lb], Atu[ua,ub],
                  e4phi, em4phi, cdphi[la], cdphi2[la,lb], g[la,lb], detg,
                  gu[ua,ub], Ats[la,lb], trAts, eta, theta,
-                 rho, S[la], trS, fac1, fac2, dottrK, dotXt[ua], epsdiss[ua]},
+                 rho, S[la], trS, fac1, fac2, dottrK, dotXt[ua]},
   Equations -> 
   {
     dir[ua] -> Sign[beta[ua]],
@@ -669,13 +683,17 @@ evolCalc =
     
     (* PRD 62, 044034 (2000), eqn. (10) *)
     (* PRD 67 084023 (2003), eqn. (16) and (23) *)
-    dot[phi]       -> IfThen [conformalMethod, 1/3 phi, -1/6] alpha trK
-                      + IfThen [conformalMethod, -1/3 phi, 1/6] PD[beta[ua],la],
+    dot[phi]       -> + IfThen [conformalMethod, 1/3 phi, -1/6]
+                        (alpha trK - PD[beta[ua],la])
+                      + Upwind[beta[ua], phi, la]
+                      + Dissipation[phi],
     
     (* PRD 62, 044034 (2000), eqn. (9) *)
     dot[gt[la,lb]] -> - 2 alpha At[la,lb]
                       + gt[la,lc] PD[beta[uc],lb] + gt[lb,lc] PD[beta[uc],la]
-                      - (2/3) gt[la,lb] PD[beta[uc],lc],
+                      - (2/3) gt[la,lb] PD[beta[uc],lc]
+                      + Upwind[beta[uc], gt[la,lb], lc]
+                      + Dissipation[gt[la,lb]],
     (* PRD 62, 044034 (2000), eqn. (20) *)
     (* PRD 67 084023 (2003), eqn (26) *)
     dotXt[ui]      -> - 2 Atu[ui,uj] PD[alpha,lj]
@@ -688,7 +706,9 @@ evolCalc =
                       + (2/3) Xtn[ui] PD[beta[uj],lj]
     (* Equation (4.28) in Baumgarte & Shapiro (Phys. Rept. 376 (2003) 41-131) *)
                       + addMatter (- 16 pi alpha gtu[ui,uj] S[lj]),
-    dot[Xt[ui]]    -> dotXt[ui],
+    dot[Xt[ui]]    -> + dotXt[ui]
+                      + Upwind[beta[ua], Xt[ui], la]
+                      + Dissipation[Xt[ui]],
 
     (* PRD 62, 044034 (2000), eqn. (11) *)
     dottrK         -> - em4phi ( gtu[ua,ub] ( PD[alpha,la,lb]
@@ -697,7 +717,9 @@ evolCalc =
                       + alpha (Atm[ua,lb] Atm[ub,la] + (1/3) trK^2)
     (* Equation (4.21) in Baumgarte & Shapiro (Phys. Rept. 376 (2003) 41-131) *)
                       + addMatter (4 pi alpha (rho + trS)),
-    dot[trK]       -> dottrK,
+    dot[trK]       -> + dottrK
+                      + Upwind[beta[ua], trK, la]
+                      + Dissipation[trK],
 
     (* PRD 62, 044034 (2000), eqn. (12) *)
     (* TODO: Should we use the Hamiltonian constraint to make Rij tracefree? *)
@@ -711,84 +733,151 @@ evolCalc =
                       - (2/3) At[la,lb] PD[beta[uc],lc]
     (* Equation (4.23) in Baumgarte & Shapiro (Phys. Rept. 376 (2003) 41-131) *)
                       + addMatter (- em4phi alpha 8 pi
-                                     (T[la,lb] - (1/3) g[la,lb] trS)),
+                                     (T[la,lb] - (1/3) g[la,lb] trS))
+                      + Upwind[beta[uc], At[la,lb], lc]
+                      + Dissipation[At[la,lb]],
     
-    (* dot[alpha] -> - harmonicF alpha^harmonicN trK, *)
-    (* dot[alpha] -> - harmonicF alpha^harmonicN A + Lie[alpha, beta], *)
-(*
-    dot[alpha] -> - harmonicF alpha^harmonicN (
-                    (1 - LapseAdvectionCoeff) A + LapseAdvectionCoeff trK)
-                  + LapseAdvectionCoeff beta[ua] PDu[alpha,la],
-
-
-    dot[A]     -> (1 - LapseAdvectionCoeff) (dottrK - AlphaDriver A),
-
-*)
-    dot[alpha] -> - harmonicF alpha^harmonicN
-                    (+ LapseACoeff       A
-                     + (1 - LapseACoeff) trK),
-
-    dot[A]     -> + LapseACoeff (dottrK - AlphaDriver A),
-
+    dot[alpha] ->     - harmonicF alpha^harmonicN IfThen[LapseACoeff, A, trK]
+                      + IfThen[LapseAdvectionCoeff,
+                               Upwind[beta[ua], alpha, la],
+                               0]
+                      + Dissipation[alpha],
+    
+    dot[A]         -> + IfThen[LapseACoeff, dottrK - AlphaDriver A, 0]
+                      + IfThen[LapseAdvectionCoeff,
+                               Upwind[beta[ua], A, la],
+                               0]
+                      + Dissipation[A],
     
     eta -> etaExpr,
     theta -> thetaExpr,
     
-    (* dot[beta[ua]] -> eta Xt[ua], *)
-    (* dot[beta[ua]] -> ShiftGammaCoeff alpha^ShiftAlphaPower B[ua], *)
     dot[beta[ua]] -> + theta ShiftGammaCoeff
-                       (+ ShiftBCoeff        B[ua]
-                        + (1 - ShiftBCoeff) (Xt[ua] - eta BetaDriver beta[ua])),
-
-    dot[B[ua]]    -> + ShiftBCoeff (dotXt[ua] - eta BetaDriver B[ua])
-
+                       IfThen[ShiftBCoeff,
+                              B[ua],
+                              Xt[ua] - eta BetaDriver beta[ua]]
+                      + IfThen[ShiftAdvectionCoeff,
+                               Upwind[beta[ub], beta[ua], lb],
+                               0]
+                      + Dissipation[beta[ua]],
+    
+    dot[B[ua]]     -> + IfThen[ShiftBCoeff, dotXt[ua] - eta BetaDriver B[ua], 0]
+                      + IfThen[ShiftAdvectionCoeff,
+                               Upwind[beta[ub], B[ua], lb],
+                               0]
+                      + Dissipation[B[ua]]
   }
 };
 
-advectCalc =
-{
-  Name -> BSSN <> "_Advect",
-  Schedule -> {"IN " <> BSSN <> "_evolCalcGroup " <>
-               "AFTER (" <> BSSN <> "_RHS1 " <> BSSN <> "_RHS2)"},
-  (*
-  Where -> Interior,
-  *)
-  (* Synchronise the RHS grid functions after this routine, so that
-     the refinement boundaries are set correctly before applying the
-     radiative boundary conditions.  *)
-  Where -> InteriorNoSync,
-  Shorthands -> {dir[ua]},
-  Equations ->
-  {
-           dir[ua] -> Sign[beta[ua]],
-
-          dot[phi] -> dot[phi] + Upwind[beta[ua], phi, la],
+tensorToString[var_] := ToString[var //. { dot[x_] -> x, Tensor[n_,__] -> n }];
 
-    dot[gt[la,lb]] -> dot[gt[la,lb]] + Upwind[beta[uc], gt[la,lb], lc],
+evolCalc1 = PartialCalculation[evolCalc, "1",
+  {
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "never"},
+                              {"split_rhs_terms", "always"}}
+  },
+  {
+    dot[trK],
+    dot[phi],
+    dot[gt[la,lb]],
+    dot[Xt[ui]],
+    dot[alpha],
+    dot[A],
+    dot[beta[ua]],
+    dot[B[ua]]
+  },
+  _, False];
 
-       dot[Xt[ui]] -> dot[Xt[ui]] + Upwind[beta[uj], Xt[ui], lj],
+evolCalc2 = PartialCalculation[evolCalc, "2",
+  {
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "never"},
+                              {"split_rhs_terms", "always"}}
+  },
+  {
+    dot[At[la,lb]]
+  },
+  _, False];
 
-          dot[trK] -> dot[trK] + Upwind[beta[ua], trK, la],
+evolCalcs = { evolCalc1, evolCalc2 };
 
-    dot[At[la,lb]] -> dot[At[la,lb]] + Upwind[beta[uc], At[la,lb], lc],
+pureCalc = PartialCalculation[evolCalc, "_pure",
+  {
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "always"},
+                              {"split_rhs_terms", "never"}}
+  },
+  {
+    dot[trK],
+    dot[phi],
+    dot[gt[la,lb]],
+    dot[Xt[ui]],
+    dot[alpha],
+    dot[A],
+    dot[beta[ua]],
+    dot[B[ua]],
+    dot[At[la,lb]]
+  },
+  term_ /; ! MatchQ[term, Dissipation[var_] | Upwind[dir_,var_,idx_]], False];
 
-        dot[alpha] -> dot[alpha] + LapseAdvectionCoeff Upwind[beta[ua], alpha, la],
+pureCalc1 = PartialCalculation[pureCalc, "1",
+  {
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "always"},
+                              {"split_rhs_terms", "always"}}
+  },
+  {
+    dot[trK],
+    dot[phi],
+    dot[gt[la,lb]],
+    dot[Xt[ui]],
+    dot[alpha],
+    dot[A],
+    dot[beta[ua]],
+    dot[B[ua]]
+  },
+  _, False];
 
-            dot[A] -> dot[A] + LapseAdvectionCoeff Upwind[beta[ua], A, la],
+pureCalc2 = PartialCalculation[pureCalc, "2",
+  {
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "always"},
+                              {"split_rhs_terms", "always"}}
+  },
+  {
+    dot[At[la,lb]]
+  },
+  _, False];
 
-     dot[beta[ua]] -> dot[beta[ua]] + ShiftAdvectionCoeff Upwind[beta[ub], beta[ua], lb],
+pureCalcs = { pureCalc1, pureCalc2 };
 
-        dot[B[ua]] -> dot[B[ua]]
-                      + ShiftBCoeff Upwind[beta[uj], Xt[ua], lj] (* take care *)
-                      + ShiftAdvectionCoeff Upwind[beta[ub], B[ua], lb]
-                      - ShiftAdvectionCoeff Upwind[beta[ub], Xt[ua], lb]
-  }
-};
+advectCalc = PartialCalculation[evolCalc, "_advect",
+  {
+    Schedule -> {"IN " <> BSSN <> "_evolCalcGroup " <>
+                 "AFTER (" <> BSSN <> "_RHS_pure " <> BSSN <> "_RHS_pure1 " <> BSSN <> "_RHS_pure2)"},
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "always"},
+                              {"split_advection_terms", "never"}}
+  },
+  {
+    dot[trK],
+    dot[phi],
+    dot[gt[la,lb]],
+    dot[Xt[ui]],
+    dot[alpha],
+    dot[A],
+    dot[beta[ua]],
+    dot[B[ua]],
+    dot[At[la,lb]]
+  },
+  Upwind[dir_,var_,idx_], True];
 
-evolCalc1 = PartialCalculation[evolCalc, "1",
+advectCalcs = Map[PartialCalculation[evolCalc, "_advect_" <> tensorToString[#],
+  {
+    Schedule -> {"IN " <> BSSN <> "_evolCalcGroup " <>
+                 "AFTER (" <> BSSN <> "_RHS_pure " <> BSSN <> "_RHS_pure1 " <> BSSN <> "_RHS_pure2)"},
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "always"},
+                              {"split_advection_terms", "always"}}
+  },
   {
-    ConditionalOnKeyword -> {"RHS_calculation", "split"}
+    #
   },
+  Upwind[dir_,var_,idx_], True]&,
   {
     dot[trK],
     dot[phi],
@@ -797,51 +886,126 @@ evolCalc1 = PartialCalculation[evolCalc, "1",
     dot[alpha],
     dot[A],
     dot[beta[ua]],
-    dot[B[ua]]
+    dot[B[ua]],
+    dot[At[la,lb]]
   }];
 
-evolCalc2 = PartialCalculation[evolCalc, "2",
+dissCalc = PartialCalculation[evolCalc, "_diss",
   {
-    ConditionalOnKeyword -> {"RHS_calculation", "split"}
+    Schedule -> {"IN " <> BSSN <> "_evolCalcGroup " <>
+                 "AFTER (" <> BSSN <> "_RHS_pure " <> BSSN <> "_RHS_pure1 " <> BSSN <> "_RHS_pure2)"},
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "always"},
+                              {"split_dissipation_terms", "never"}}
   },
   {
+    dot[trK],
+    dot[phi],
+    dot[gt[la,lb]],
+    dot[Xt[ui]],
+    dot[alpha],
+    dot[A],
+    dot[beta[ua]],
+    dot[B[ua]],
+    dot[At[la,lb]]
+  },
+  Dissipation[var_], True];
+
+dissCalcs = Map[PartialCalculation[evolCalc, "_diss_" <> tensorToString[#],
+  {
+    Schedule -> {"IN " <> BSSN <> "_evolCalcGroup " <>
+                 "AFTER (" <> BSSN <> "_RHS_pure " <> BSSN <> "_RHS_pure1 " <> BSSN <> "_RHS_pure2)"},
+    ConditionalOnKeywords -> {{"split_rhs_advection_dissipation", "always"},
+                              {"split_dissipation_terms", "always"}}
+  },
+  {
+    #
+  },
+  Dissipation[var_], True]&,
+  {
+    dot[trK],
+    dot[phi],
+    dot[gt[la,lb]],
+    dot[Xt[ui]],
+    dot[alpha],
+    dot[A],
+    dot[beta[ua]],
+    dot[B[ua]],
     dot[At[la,lb]]
   }];
 
-dissCalc =
+(******************************************************************************)
+(* Advection terms *)
+(******************************************************************************)
+
+advectCalcUNUSED =
 {
-  Name -> BSSN <> "_Dissipation",
+  Name -> BSSN <> "_Advection",
   Schedule -> {"IN " <> BSSN <> "_evolCalcGroup " <>
                "AFTER (" <> BSSN <> "_RHS1 " <> BSSN <> "_RHS2)"},
-  ConditionalOnKeyword -> {"apply_dissipation", "always"},
+  (*
+  Where -> Interior,
+  *)
+  (* Synchronise the RHS grid functions after this routine, so that
+     the refinement boundaries are set correctly before applying the
+     radiative boundary conditions.  *)
   Where -> InteriorNoSync,
-  Shorthands -> {epsdiss[ua]},
+  Shorthands -> {dir[ua]},
   Equations ->
   {
-    epsdiss[ua] -> EpsDiss,
-    Sequence@@Table[
-      dot[var]       -> dot[var] + epsdiss[ux] PDdiss[var,lx],
-      {var, {phi, gt[la,lb], Xt[ui], trK, At[la,lb], alpha, A, beta[ua], B[ua]}}]
+    dir[ua]        -> Sign[beta[ua]],
+    
+    dot[phi]       -> dot[phi] + Upwind[beta[ua], phi, la],
+    
+    dot[gt[la,lb]] -> dot[gt[la,lb]] + Upwind[beta[uc], gt[la,lb], lc],
+    
+    dot[Xt[ui]]    -> dot[Xt[ui]] + Upwind[beta[uj], Xt[ui], lj],
+    
+    dot[trK]       -> dot[trK] + Upwind[beta[ua], trK, la],
+    
+    dot[At[la,lb]] -> dot[At[la,lb]] + Upwind[beta[uc], At[la,lb], lc],
+    
+    dot[alpha]     -> dot[alpha] + IfThen[LapseAdvectionCoeff,
+                                          Upwind[beta[ua], alpha, la],
+                                          0],
+    
+    dot[A]         -> dot[A] + IfThen[LapseAdvectionCoeff,
+                                      Upwind[beta[ua], A, la],
+                                      0],
+    
+    dot[beta[ua]]  -> dot[beta[ua]] + IfThen[ShiftAdvectionCoeff,
+                                             Upwind[beta[ub], beta[ua], lb],
+                                             0],
+    
+    dot[B[ua]]     -> dot[B[ua]] + IfThen[ShiftAdvectionCoeff,
+                                          + Upwind[beta[ub], B[ua], lb]
+                                          - Upwind[beta[ub], Xt[ua], lb],
+                                          0]
   }
 };
 
-dissCalcs =
-Table[
+(******************************************************************************)
+(* Dissipation *)
+(******************************************************************************)
+
+dissCalcUNUSED =
 {
-  Name -> BSSN <> "_Dissipation_" <> ToString[var /. {Tensor[n_,__] -> n}],
+  Name -> BSSN <> "_Dissipation",
   Schedule -> {"IN " <> BSSN <> "_evolCalcGroup " <>
                "AFTER (" <> BSSN <> "_RHS1 " <> BSSN <> "_RHS2)"},
   ConditionalOnKeyword -> {"apply_dissipation", "always"},
   Where -> InteriorNoSync,
-  Shorthands -> {epsdiss[ua]},
   Equations ->
   {
-    epsdiss[ua] -> EpsDiss,
-    dot[var]    -> dot[var] + epsdiss[ux] PDdiss[var,lx]
+    Sequence@@Table[
+      dot[var] -> dot[var] + Dissipation[var],
+      {var, {phi, gt[la,lb], Xt[ui], trK, At[la,lb],
+             alpha, A, beta[ua], B[ua]}}]
   }
-},
-  {var, {phi, gt[la,lb], Xt[ui], trK, At[la,lb], alpha, A, beta[ua], B[ua]}}
-];
+};
+
+(******************************************************************************)
+(* Boundaries *)
+(******************************************************************************)
 
 RHSStaticBoundaryCalc =
 {
@@ -924,6 +1088,10 @@ RHSRadiativeBoundaryCalc =
   }
 };
 
+(******************************************************************************)
+(* Algebraic constraints *)
+(******************************************************************************)
+
 enforceCalc =
 {
   Name -> BSSN <> "_enforce",
@@ -983,6 +1151,7 @@ constraintsCalc =
 {
   Name -> BSSN <> "_constraints",
   Schedule -> Automatic,
+  ConditionalOnKeyword -> { "split_constraint_terms", "never" },
   After -> "MoL_PostStep",
   Where -> Interior,
   Shorthands -> {detgt, ddetgt[la], gtu[ua,ub],
@@ -1109,19 +1278,27 @@ constraintsCalc =
 };
 
 constraintsCalc1 = PartialCalculation[constraintsCalc, "1",
-  {},
+  {
+    ConditionalOnKeyword -> { "split_constraint_terms", "always" }
+  },
   {
     H
-  }];
+  },
+  _, False];
 
 constraintsCalc2 = PartialCalculation[constraintsCalc, "2",
-  {},
+  {
+    ConditionalOnKeyword -> { "split_constraint_terms", "always" }
+  },
   {
     M[li],
     cS,
     cXt[ua],
     cA
-  }];
+  },
+  _, False];
+
+constraintsCalcs = { constraintsCalc1, constraintsCalc2 };
 
 (******************************************************************************)
 (* Implementations *)
@@ -1214,6 +1391,42 @@ keywordParameters =
     Default -> "correct"
   },
   {
+    Name -> "split_rhs_advection_dissipation",
+    Description -> "Whether to split RHS, advection, and dissipation terms into separate routines",
+    AllowedValues -> {"always",
+                      "never" (* yes and no keyword values confuse Cactus, and Kranc
+                                 doesn't support boolean parameters *)
+                     },
+    Default -> "always"
+  },
+  {
+    Name -> "split_rhs_terms",
+    Description -> "Whether to split the individual RHS terms over multiple routines",
+    AllowedValues -> {"always",
+                      "never" (* yes and no keyword values confuse Cactus, and Kranc
+                                 doesn't support boolean parameters *)
+                     },
+    Default -> "always"
+  },
+  {
+    Name -> "split_advection_terms",
+    Description -> "Whether to split the individual advection terms over multiple routines",
+    AllowedValues -> {"always",
+                      "never" (* yes and no keyword values confuse Cactus, and Kranc
+                                 doesn't support boolean parameters *)
+                     },
+    Default -> "always"
+  },
+  {
+    Name -> "split_dissipation_terms",
+    Description -> "Whether to split the individual dissipation terms over multiple routines",
+    AllowedValues -> {"always",
+                      "never" (* yes and no keyword values confuse Cactus, and Kranc
+                                 doesn't support boolean parameters *)
+                     },
+    Default -> "always"
+  },
+  {
     Name -> "apply_dissipation",
     Description -> "Whether to apply dissipation to the RHSs",
     AllowedValues -> {"always",
@@ -1221,6 +1434,15 @@ keywordParameters =
                                  doesn't support boolean parameters *)
                      },
     Default -> "always"
+  },
+  {
+    Name -> "split_constraint_terms",
+    Description -> "Whether to split the individual constraint terms over multiple routines",
+    AllowedValues -> {"always",
+                      "never" (* yes and no keyword values confuse Cactus, and Kranc
+                                 doesn't support boolean parameters *)
+                     },
+    Default -> "always"
   }
 
 };
@@ -1242,17 +1464,32 @@ intParameters =
     AllowedValues -> {{Value -> "0", Description -> "phi method"},
                       {Value -> "1", Description -> "W method"}},
     Default -> 0
-  }
-};
-
-realParameters =
-{
+  },
   {
     Name -> LapseACoeff,
     Description -> "Whether to evolve A in time",
     Default -> 0
   },
   {
+    Name -> ShiftBCoeff,
+    Description -> "Whether to evolve B^i in time",
+    Default -> 1
+  },
+  {
+    Name -> LapseAdvectionCoeff,
+    Description -> "Factor in front of the shift advection terms in 1+log",
+    Default -> 1
+  },
+  {
+    Name -> ShiftAdvectionCoeff,
+    Description -> "Factor in front of the shift advection terms in gamma driver",
+    Default -> 1
+  }
+};
+
+realParameters =
+{
+  {
     Name -> harmonicF,
     Description -> "d/dt alpha = - f alpha^n K   (harmonic=1, 1+log=2)",
     Default -> 1
@@ -1262,11 +1499,6 @@ realParameters =
     Default -> 0
   },
   {
-    Name -> ShiftBCoeff,
-    Description -> "Whether to evolve B^i in time",
-    Default -> 1
-  },
-  {
     Name -> ShiftGammaCoeff,
     Default -> 0
   },
@@ -1275,16 +1507,6 @@ realParameters =
     Default -> 0
   },
   {
-    Name -> LapseAdvectionCoeff,
-    Description -> "Factor in front of the shift advection terms in 1+log",
-    Default -> 1
-  },
-  {
-    Name -> ShiftAdvectionCoeff,
-    Description -> "Factor in front of the shift advection terms in gamma driver",
-    Default -> 1
-  },
-  {
     Name -> MinimumLapse,
     Description -> "Minimum value of the lapse function",
     Default -> -1
@@ -1320,12 +1542,11 @@ Join[
   convertFromADMBaseCalc,
   initGammaCalc,
   convertFromADMBaseGammaCalc,
-  (* evolCalc, *)
-  evolCalc1, evolCalc2,
-  dissCalc,
+  evolCalc,
+  pureCalc,
   advectCalc,
+  dissCalc,
   initRHSCalc,
-  (* evol1Calc, evol2Calc, *)
   RHSStaticBoundaryCalc,
   (* RHSRadiativeBoundaryCalc, *)
   enforceCalc,
@@ -1334,10 +1555,13 @@ Join[
   convertToADMBaseDtLapseShiftCalc,
   convertToADMBaseDtLapseShiftBoundaryCalc,
   convertToADMBaseFakeDtLapseShiftCalc,
-  (* constraintsCalc, *)
-  constraintsCalc1, constraintsCalc2
+  constraintsCalc
 },
-  {} (*dissCalcs*)
+evolCalcs,
+pureCalcs,
+advectCalcs,
+dissCalcs,
+constraintsCalcs
 ];
 
 CreateKrancThornTT [groups, ".", BSSN,
@@ -1346,14 +1570,17 @@ CreateKrancThornTT [groups, ".", BSSN,
   PartialDerivatives -> derivatives,
   EvolutionTimelevels -> evolutionTimelevels,
   DefaultEvolutionTimelevels -> 3,
+  CSE -> True,
   UseJacobian -> useJacobian,
   UseLoopControl -> True,
+  UseVectors -> True,
   InheritedImplementations -> inheritedImplementations,
   InheritedKeywordParameters -> inheritedKeywordParameters,
   ExtendedKeywordParameters -> extendedKeywordParameters,
   KeywordParameters -> keywordParameters,
   IntParameters -> intParameters,
-  RealParameters -> realParameters
+  RealParameters -> realParameters,
+  UseJacobian -> useJacobian
 ];
 
 ];
