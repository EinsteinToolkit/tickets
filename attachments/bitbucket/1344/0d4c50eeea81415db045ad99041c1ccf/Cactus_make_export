Index: make/make.config.defn.in
===================================================================
--- make/make.config.defn.in	(revision 4997)
+++ make/make.config.defn.in	(working copy)
@@ -242,58 +242,58 @@
 export CCTK_DEBUG_MODE = @DEBUG_MODE@
 
 ifeq ($(strip $(CCTK_DEBUG_MODE)),yes)
-  CPPFLAGS  += $(CPP_DEBUG_FLAGS)
-  FPPFLAGS  += $(FPP_DEBUG_FLAGS)
-  CFLAGS    += $(C_DEBUG_FLAGS)
-  CXXFLAGS  += $(CXX_DEBUG_FLAGS)
-  CUCCFLAGS += $(CUCC_DEBUG_FLAGS)
-  F77FLAGS  += $(F77_DEBUG_FLAGS)
-  F90FLAGS  += $(F90_DEBUG_FLAGS)
+  export CPPFLAGS  += $(CPP_DEBUG_FLAGS)
+  export FPPFLAGS  += $(FPP_DEBUG_FLAGS)
+  export CFLAGS    += $(C_DEBUG_FLAGS)
+  export CXXFLAGS  += $(CXX_DEBUG_FLAGS)
+  export CUCCFLAGS += $(CUCC_DEBUG_FLAGS)
+  export F77FLAGS  += $(F77_DEBUG_FLAGS)
+  export F90FLAGS  += $(F90_DEBUG_FLAGS)
 endif
 
 # Is this an optimising configuration ?
 CCTK_OPTIMISE_MODE = @OPTIMISE_MODE@
 
 ifeq ($(strip $(CCTK_OPTIMISE_MODE)),yes)
-  CPPFLAGS  += $(CPP_OPTIMISE_FLAGS)
-  FPPFLAGS  += $(FPP_OPTIMISE_FLAGS)
-  CFLAGS    += $(C_OPTIMISE_FLAGS)
-  CXXFLAGS  += $(CXX_OPTIMISE_FLAGS)
-  CUCCFLAGS += $(CUCC_OPTIMISE_FLAGS)
-  F77FLAGS  += $(F77_OPTIMISE_FLAGS)
-  F90FLAGS  += $(F90_OPTIMISE_FLAGS)
+  export CPPFLAGS  += $(CPP_OPTIMISE_FLAGS)
+  export FPPFLAGS  += $(FPP_OPTIMISE_FLAGS)
+  export CFLAGS    += $(C_OPTIMISE_FLAGS)
+  export CXXFLAGS  += $(CXX_OPTIMISE_FLAGS)
+  export CUCCFLAGS += $(CUCC_OPTIMISE_FLAGS)
+  export F77FLAGS  += $(F77_OPTIMISE_FLAGS)
+  export F90FLAGS  += $(F90_OPTIMISE_FLAGS)
 else
-  CFLAGS    += $(C_NO_OPTIMISE_FLAGS)
-  CXXFLAGS  += $(CXX_NO_OPTIMISE_FLAGS)
-  CUCCFLAGS += $(CUCC_NO_OPTIMISE_FLAGS)
-  F77FLAGS  += $(F77_NO_OPTIMISE_FLAGS)
-  F90FLAGS  += $(F90_NO_OPTIMISE_FLAGS)
+  export CFLAGS    += $(C_NO_OPTIMISE_FLAGS)
+  export CXXFLAGS  += $(CXX_NO_OPTIMISE_FLAGS)
+  export CUCCFLAGS += $(CUCC_NO_OPTIMISE_FLAGS)
+  export F77FLAGS  += $(F77_NO_OPTIMISE_FLAGS)
+  export F90FLAGS  += $(F90_NO_OPTIMISE_FLAGS)
 endif
 
 # Is this a profiling configuration ?
 CCTK_PROFILE_MODE = @PROFILE_MODE@
 
 ifeq ($(strip $(CCTK_PROFILE_MODE)),yes)
-  CPPFLAGS  += $(CPP_PROFILE_FLAGS)
-  FPPFLAGS  += $(FPP_PROFILE_FLAGS)
-  CFLAGS    += $(C_PROFILE_FLAGS)
-  CXXFLAGS  += $(CXX_PROFILE_FLAGS)
-  CUCCFLAGS += $(CUCC_PROFILE_FLAGS)
-  F77FLAGS  += $(F77_PROFILE_FLAGS)
-  F90FLAGS  += $(F90_PROFILE_FLAGS)
+  export CPPFLAGS  += $(CPP_PROFILE_FLAGS)
+  export FPPFLAGS  += $(FPP_PROFILE_FLAGS)
+  export CFLAGS    += $(C_PROFILE_FLAGS)
+  export CXXFLAGS  += $(CXX_PROFILE_FLAGS)
+  export CUCCFLAGS += $(CUCC_PROFILE_FLAGS)
+  export F77FLAGS  += $(F77_PROFILE_FLAGS)
+  export F90FLAGS  += $(F90_PROFILE_FLAGS)
 endif
 
 # Are we building with OpenMP support ?
 CCTK_OPENMP_MODE = @OPENMP_MODE@
 
 ifeq ($(strip $(CCTK_OPENMP_MODE)),yes)
-  CPPFLAGS  += $(CPP_OPENMP_FLAGS)
-  FPPFLAGS  += $(FPP_OPENMP_FLAGS)
-  CFLAGS    += $(C_OPENMP_FLAGS)
-  CXXFLAGS  += $(CXX_OPENMP_FLAGS)
-  CUCCFLAGS += $(CUCC_OPENMP_FLAGS)
-  F77FLAGS  += $(F77_OPENMP_FLAGS)
-  F90FLAGS  += $(F90_OPENMP_FLAGS)
+  export CPPFLAGS  += $(CPP_OPENMP_FLAGS)
+  export FPPFLAGS  += $(FPP_OPENMP_FLAGS)
+  export CFLAGS    += $(C_OPENMP_FLAGS)
+  export CXXFLAGS  += $(CXX_OPENMP_FLAGS)
+  export CUCCFLAGS += $(CUCC_OPENMP_FLAGS)
+  export F77FLAGS  += $(F77_OPENMP_FLAGS)
+  export F90FLAGS  += $(F90_OPENMP_FLAGS)
 endif
 
 # Are we building with all warnings switched on ?
@@ -305,13 +305,13 @@
 endif
 
 ifeq ($(strip $(CCTK_WARN_MODE)),yes)
-  CPPFLAGS  += $(CPP_WARN_FLAGS)
-  FPPFLAGS  += $(FPP_WARN_FLAGS)
-  CFLAGS    += $(C_WARN_FLAGS)
-  CXXFLAGS  += $(CXX_WARN_FLAGS)
-  CUCCFLAGS += $(CUCC_WARN_FLAGS)
-  F77FLAGS  += $(F77_WARN_FLAGS)
-  F90FLAGS  += $(F90_WARN_FLAGS)
+  export CPPFLAGS  += $(CPP_WARN_FLAGS)
+  export FPPFLAGS  += $(FPP_WARN_FLAGS)
+  export CFLAGS    += $(C_WARN_FLAGS)
+  export CXXFLAGS  += $(CXX_WARN_FLAGS)
+  export CUCCFLAGS += $(CUCC_WARN_FLAGS)
+  export F77FLAGS  += $(F77_WARN_FLAGS)
+  export F90FLAGS  += $(F90_WARN_FLAGS)
 endif
 
 # Stuff specific to an architecture.
