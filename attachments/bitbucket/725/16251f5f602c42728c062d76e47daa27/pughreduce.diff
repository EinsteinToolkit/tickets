Index: ReductionAvg.c
===================================================================
--- ReductionAvg.c	(revision 93)
+++ ReductionAvg.c	(working copy)
@@ -324,9 +324,9 @@
     }
   }
 
-#ifdef CCTK_MPI
   pughGH = PUGH_pGH (GH);
 
+#ifdef CCTK_MPI
   /* do the global reduction from the processors' local reduction results */
   if (pughGH->nprocs > 1)
   {

