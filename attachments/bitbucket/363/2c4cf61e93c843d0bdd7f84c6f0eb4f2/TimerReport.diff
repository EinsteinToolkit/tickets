Index: param.ccl
===================================================================
--- param.ccl	(revision 37)
+++ param.ccl	(working copy)
@@ -52,6 +52,12 @@
   1:* :: "Any number of timers"
 } 0
 
+INT n_top_timers_combined "How many timers (that have been combined over all processes) to include in the top timer report" STEERABLE=ALWAYS
+{
+  0   :: "Do not print the report"
+  1:* :: "Any number of timers"
+} 0
+
 shares: IO
 
 USES STRING out_dir
Index: src/Output.c
===================================================================
--- src/Output.c	(revision 37)
+++ src/Output.c	(working copy)
@@ -41,12 +41,15 @@
  ********************* Local Routine Prototypes *********************
  ********************************************************************/
 
+static int min(int x, int y);
+
 static void Output (CCTK_ARGUMENTS);
 static void PrintTimes (CCTK_ARGUMENTS);
 static void OutputAllTimers (CCTK_ARGUMENTS);
 static void OutputAllTimersTogether (CCTK_ARGUMENTS);
 static void OutputAllTimersReadable (CCTK_ARGUMENTS);
 static void PrintTopTimers (CCTK_ARGUMENTS);
+static void PrintTopTimersCombined (CCTK_ARGUMENTS);
 
 static int CollectTimerInfo (cGH const * restrict const cctkGH,
                              struct timer_stats * restrict const timers);
@@ -70,6 +73,8 @@
  *********************     Local Data   *****************************
  ********************************************************************/
 
+static const char *const sep = "======================================================================";
+
 /********************************************************************
  ********************    External Routines   ************************
  ********************************************************************/
@@ -153,8 +158,13 @@
  ********************    Internal Routines   ************************
  ********************************************************************/
 
+static int min(int x, int y)
+{
+  return x<y ? x : y;
+}
 
 
+
 static void Output (CCTK_ARGUMENTS)
 {
   DECLARE_CCTK_ARGUMENTS;
@@ -181,6 +191,11 @@
   {
     PrintTopTimers(CCTK_PASS_CTOC);
   }
+
+  if (n_top_timers_combined > 0)
+  {
+    PrintTopTimersCombined(CCTK_PASS_CTOC);
+  }
 }
 
 
@@ -556,8 +571,6 @@
   double total = 0;
   int i;
 
-  const char *sep = "======================================================================";
-
   assert(ntimers >= 0);
   for (i = 0; i < ntimers; i++)
   {
@@ -596,7 +609,7 @@
   /* This should be the "CCTK total time" timer */
   total = timers[0].t;
 
-  for (i = 0; i < (ntimers >= topn ? topn : ntimers) ; i++)
+  for (i = 0; i < min(ntimers, topn); i++)
   {
     double percent = 100.0 * timers[i].t / total;
 
@@ -614,6 +627,85 @@
   return;
 }
 
+
+
+static struct timer_stats const * compare_combined_timers = NULL;
+
+static int compare_combined(const void *a, const void *b)
+{
+  int const idx1 = *(const int *)a;
+  int const idx2 = *(const int *)b;
+  
+  /* compare average times */
+  double const d =
+    compare_combined_timers->secs_avg[idx2] -
+    compare_combined_timers->secs_avg[idx1];
+  
+  if (d > 0)
+    return +1;
+  else if (d < 0)
+    return -1;
+  else
+    return 0;
+}
+
+static void PrintTopTimersCombined (CCTK_ARGUMENTS)
+{
+  DECLARE_CCTK_ARGUMENTS;
+  DECLARE_CCTK_PARAMETERS;
+  
+  /* Collect timing information from all processes */
+  struct timer_stats timers;
+  if (! CollectTimerInfo (cctkGH, &timers))
+  {
+    return;
+  }
+  
+  /* Sort timers (by average) */
+  int idx[timers.ntimers];
+  for (int i=0; i<timers.ntimers; ++i)
+  {
+    idx[i] = i;
+  }
+  compare_combined_timers = &timers;
+  qsort (idx, timers.ntimers, sizeof(*idx), compare_combined);
+  compare_combined_timers = NULL;
+  
+  CCTK_VInfo(CCTK_THORNSTRING,
+             "Top combined timers at iteration %d time %g",
+             cctk_iteration, (double)cctk_time);
+  
+  printf("%s\n", sep);
+  printf("%5s   %7s %7s %7s   %s (%s)\n",
+         "%", "Time/s", "Min/s", "Max/s", "Timer", all_timers_clock);
+  printf("%s\n", sep);
+  
+  /* This should be the "CCTK total time" timer */
+  int const total_idx = 0;
+  assert (timers.ntimers > total_idx);
+  
+  CCTK_REAL const max_time = timers.secs_max[idx[total_idx]];
+  int const digits = floor(log10(max_time) + 1.0e-4) + 1;
+  
+  /* Output timing results */
+  for (int i=0; i<min(timers.ntimers, n_top_timers_combined); ++i)
+  {
+    double const percent =
+      100.0 * timers.secs_avg[idx[i]] / timers.secs_avg[idx[total_idx]];
+    
+    /* field widths: 5+3 + 7+1 + 7+1 + 7+3 + n = 80 */
+    printf("%5.1f   %7.*f %7.*f %7.*f   %s\n",
+           percent,
+           6-digits, timers.secs_avg[idx[i]],
+           6-digits, timers.secs_min[idx[i]],
+           6-digits, timers.secs_max[idx[i]],
+           CCTK_TimerName(idx[i]));
+  }
+  printf("%s\n", sep);
+}
+
+
+
 static int integer_same_on_all_procs(cGH const * restrict const cctkGH, const CCTK_INT i)
 {
   /* There is no "equals" reduction operator, so we check that
