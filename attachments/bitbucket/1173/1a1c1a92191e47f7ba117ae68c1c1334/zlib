Index: configure.sh
===================================================================
--- configure.sh	(revision 25)
+++ configure.sh	(working copy)
@@ -170,7 +170,7 @@
 ################################################################################
 
 # Set options
-if [ "${ZLIB_DIR}" = '/usr' -o "${ZLIB_DIR}" = '/usr/local' ]; then
+if [ "${ZLIB_DIR}" = '/usr' -o "${ZLIB_DIR}" = '/usr/local' -o "${ZLIB_DIR}" = 'NO_BUILD' ]; then
     ZLIB_INC_DIRS=''
     ZLIB_LIB_DIRS=''
 else
